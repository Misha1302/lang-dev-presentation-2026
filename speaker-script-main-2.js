Object.assign(window.SPEAKER_NOTES, {
  "m24": "Features may depend on other features. Several providers may satisfy one capability. Passes can have ordering or mandatory constraints. Policy can exclude features or restrict backends and targets. So the planner is not merely finding a shortest connected path.\n\nOnce that choice is resolved, we freeze it.",
  "m25": "The LanguagePlan records the resolved feature set, contributions and providers, a route for each enabled backend, the entry artifact contract, the selected runtime provider, and a stable PlanHash. In current UT, plan construction verifies the resulting declared artifact routes. The hash identifies the resolved snapshot; it does not prove semantic equivalence.\n\nWhy bother freezing this answer?",
  "m26": "Because composition is an upfront responsibility. During authoring and planning we resolve modules, dependencies, providers, and routes. At execution time, one resolved plan already contains preplanned routes for the enabled backends, and each execution chooses one. This does not mean zero overhead; it means we do not reopen the global composition problem for every run.\n\nReflection in the current implementation obeys the same boundary.",
  "m26r": "Current UT does use reflection, but only after planning has selected the exact implementation types. A bounded resolver inspects provider attributes and known provider interfaces on that finite set and builds typed capability descriptors. Reflection cannot add a feature, choose a backend, or change a route. It removes wiring. The LanguagePlan removes ambiguity.\n\nSo open-world authoring does not force open-world execution.",
  "m27": "A common fear is that extensibility means permanent plugin boundaries, runtime indirection, and only local optimization. That outcome is possible — but it is not required by the architecture.\n\nWe can separate the time when extensibility is needed from the time when optimization happens.",
  "m28": "Independent modules matter while we author and compose the language. After dependencies and policy are resolved, we can freeze one concrete compiler. From that point, global analyses can work over the unified representations that actually exist in that compiler.\n\nSo module boundaries do not have to become optimization boundaries.",
  "m29": "After resolution we know the backend routes, concrete representations, selected passes, and providers. The design objective is that global analyses and optimizations can see the unified IR they need. I am not claiming a measured speedup here — only that extensible authoring should not architecturally forbid global transformation.\n\nThat raises a second planning question: how should users ask for optimization?",
  "m30": "Ideally, a user asks for something like aggressive optimization, not for an exact recipe of canonicalization, SSA formation, legality preparation, and target-specific passes. The syntax here is a design sketch, not current Wist syntax.\n\nLLVM already gives us a familiar version of this idea.",
  "m31": "In LLVM, an optimization level such as O2 or O3 is a high-level request. PassBuilder constructs a concrete default optimization pipeline and exposes extension points. I am not claiming UT reproduces LLVM's O3 machinery. The useful intuition is simply high-level policy producing a derived concrete pipeline.\n\nBut deriving a pipeline can require representation prerequisites.",
  "m32": "An optimization may require some property P first: perhaps an SSA-like form, normalized layout, or target legality. So the planner may need to choose a conversion or legalization before the optimization becomes applicable. This is a hypothetical general example.\n\nAnd this connects to a concrete lesson from the current planner.",
  "m33": "An older version of the UT planner made a useful mistake. It could choose the cheapest conversion topology first, and only afterward try to place a mandatory pass. That can miss a globally feasible route. The current planner fixes this by including mandatory-pass coverage during route search, before preference decides among candidates.\n\nThe general rule is simple.",
  "m34": "Feasibility first. Preference second. Hard constraints — declared route compatibility, mandatory constraints, ambiguity handling — decide what is admissible. Cost, order, and policy preferences rank only the candidates that remain.\n\nAnd even then, current feasibility is only structural.",
  "m35": "In current UT, Route Cost is just a declared preference weight. The planner checks declared artifact-contract compatibility and mandatory-pass feasibility, but matching contract identities do not prove that independently authored components preserve identical meaning. Cost proves neither semantic preservation nor trust. This is exactly where structural planning reaches its current boundary.\n\nA more general planner may need requirements richer than cost.",
  "m36": "Imagine route A is cheaper and reaches the backend, but destroys property P that an optimization explicitly needs. Route B costs more but preserves the required form. If P is a real requirement, A is not cheaper — it is inadmissible. Preference should only compare plans that satisfy the requirements.\n\nThat gives us the architectural payoff of the first half.",
  "m37": "So the target is: deep abstractions while authoring, explicit configuration, planning, one frozen plan, then shared representations and global optimization where the compiler allows it. Resolution should remove composition ambiguity without turning module boundaries into permanent optimization boundaries.\n\nNow I want to show two places where current UT already gives us concrete evidence for that direction.",
  "m38": "Here is a small but concrete example. In AIR, an exact pattern — load the environment, push a known slot, then load an external value of a known type — can be rewritten, when the backend supports it, into one typed intrinsic carrying the slot directly. Three representation steps become one. The representation machinery disappears; the external-load semantics do not.\n\nThe second witness is semantic parity across different backends.",
  "m39": "The interpreter and CIL backend use different representations, but they share the same upstream binding rule. In this example, a local `price` shadows an external `price`, and both backends preserve that meaning. The regression is relational evidence for the tested programs, not a proof of equivalence for every Wist program. Backend diversity is allowed; accidental semantic diversity is not.\n\nAnd this suggests a deeper question.",
  "m40": "Current UT already has a structural shape where a selected module can expose a typed capability provider without the generic layer hard-coding that module. The research question is whether a similarly stable boundary can carry richer semantic knowledge. This is an architectural analogy, not a claim that the proposed semantic substrate already exists.\n\nReturn to our two independent extension authors.",
  "m41": "Suppose the type or refinement module proves that an index is in some range. A separately authored bounds checker may be able to use that information. The interesting question is: how can the consumer ask for the knowledge without depending on the concrete producer?\n\nThe naive answer scales badly.",
  "m42": "If every range, shape, ownership, type, or effects analysis needs a private adapter to every optimizer, safety checker, IDE feature, vectorizer, or backend, integration grows pairwise. That is the coupling I want to attack.\n\nSo here is the falsifiable design target.",
  "m43": "Add a new semantic producer, and change zero existing consumers. If the new producer can strengthen valid answers through a shared boundary, we have evidence of real producer independence.\n\nThat shared boundary is what I mean by a semantic contract layer.",
  "m44": "The proposed bridge is a shared, typed, versioned semantic contract: producers publish knowledge through stable query schemas, and consumers ask for what they need through the same boundary. This is a research hypothesis. It is not current UT implementation, and it does not require one universal ontology.\n\nA bounds-check example makes the idea less abstract.",
  "m45": "Range analysis may know that `i` is between zero and seven. Shape or extent analysis may know that the length of `a` is ten. Neither producer needs to know about bounds lowering. A shared rule can combine those facts to prove `0 <= i < length(a)`, discharge a SafeIndex obligation, and allow the bounds check to be omitted.\n\nBut I want to keep the implementation boundary very explicit."
});
