Object.assign(window.SPEAKER_NOTES, {
  "m46": "Current UT gives us modules, dialect configuration, dependency and provider resolution, artifact routes, frozen plans, and the concrete rewrite and parity examples I just showed. Judgements, semantic obligations, and generalized cross-level projection are proposed research. I do not want the prototype to silently turn into evidence for claims it does not implement.\n\nNow we can ask what a semantic fact has to contain to be safe.",
  "m47": "A naked fact such as `range(i) = [0,n)` is not enough. After a transformation, what exactly is `i`? Is it the same entity? In the same context? Has the transformation invalidated the analysis? A useful semantic system has to represent those questions explicitly.\n\nThat motivates the first proposed concept: a contextual Judgement.",
  "m48": "I use the word Judgement for a contextual claim: a subject and value, plus context, revision or validity, evidence, and assumptions. The exact schema is research work. The important part is that a consumer should be able to ask not only “what do we know?” but also “where did this answer come from, and is it still valid here?”\n\nWhy not just encode all semantics in a normal type hierarchy?",
  "m49": "Consider a write operation. Volatility, atomicity, ordering, visibility, garbage-collector barrier obligations, and transaction context are distinct, partly orthogonal concerns. Some interact, but forcing their cross-product into one inheritance lattice quickly creates artificial types instead of useful semantic views.\n\nSo I would keep the stable identity layer deliberately small.",
  "m50": "A possible direction is a small set of stable anchors — Value, Place, Operation, Region, Type, Symbol — with extensible semantic schemas around them. These are candidate anchors, not a finished universal ontology. The stable part should be only as large as composition really needs.\n\nThen consumers can ask questions about operations directly.",
  "m51": "Instead of asking whether an object belongs to a giant subclass such as AtomicVolatileSomethingWritable, ask typed questions about `Write(place, value)`: can it write, what are the effects, what ordering is required, what visibility semantics apply? One operation can expose several semantic views.\n\nKnowledge by itself, however, is not yet a correctness decision.",
  "m52": "Judgements represent what we know. Obligations represent what must hold before a transformation, check elimination, or lowering decision is legal. A decision is justified only when the relevant obligations have been discharged by valid evidence.\n\nAnd some obligations really do constrain route structure.",
  "m53": "For example, a reference store may have to preserve required GC-barrier semantics. That obligation restricts which lowerings are legal: the selected path must retain or emit the required behavior. Only after legality is established should cost or target policy rank the feasible candidates. Again, this is a proposed planner model, not current UT behavior.\n\nA shared contract boundary still does not imply one giant algorithm.",
  "m54": "Different questions should still use different engines: abstract interpretation, fixed points, graph search, legalization, dependency resolution, cost selection, validation. The shared layer is about typed semantic boundaries and responsibility, not about forcing every problem into one solver.\n\nThe next difficulty appears when representations change.",
  "m55": "Lowering makes representation more concrete. That does not mean semantic knowledge becomes more precise in the same direction. Knowledge may become more precise, less precise, or simply move to a different vocabulary. Representation level and semantic precision are separate axes.\n\nThat matters because high-level operations can disappear.",
  "m56": "A high-level `Write(place, value)` may lower to a target store, a store plus a barrier, a runtime helper, or several machine operations. These are alternatives or compositions, not one mandatory chain. Later, some consumer may still need to know what write semantics must hold even though the original node is gone.\n\nThe obvious answer — reconstruct the old IR — does not generally work.",
  "m57": "Transformations split, merge, fuse, delete, and perform many-to-one lowering. In general they are not invertible enough to rebuild the old representation faithfully. So universal inverse lowering is the wrong abstraction.\n\nThe alternative I want to explore is upward semantic projection.",
  "m58": "By upward semantic projection I mean something narrower than rebuilding the old IR. Lower-level artifacts should be able to answer selected higher-level semantic queries — for example, “what write semantics must still hold here?” — using provenance, semantic views, or re-analysis. The representation stays lowered; selected meaning remains queryable. This is a research hypothesis.\n\nThere probably is not one mechanism that does all of this.",
  "m59": "Projection may be hybrid. Provenance can track where artifacts came from. Lowered nodes can expose semantic interfaces or views. Some properties can be re-derived by analysis on the concrete IR. Every mechanism still needs the same discipline around revision, validity, assumptions, and evidence.\n\nThis direction is not emerging in a vacuum, so let me position it against familiar prior art.",
  "m60": "LLVM is strong prior art for separating analyses, transformations, concrete pass pipelines, and extension points. PassBuilder constructs default pipelines for optimization levels, and plugins can inject passes at documented extension points. The lesson I borrow is that extensible pass infrastructure can still produce one concrete optimization pipeline.\n\nMLIR gives an even closer analogy for semantics across extensible IR.",
  "m61": "MLIR makes extensible operations and multiple abstraction levels first-class. Interfaces let generic transformations query dialect-defined semantics, while dialect conversion uses explicit legality targets, rewrite patterns, and optional type conversion. The lesson is that extensible IR needs explicit semantic interfaces and legalization boundaries. And again, Wist's word “dialect” means something different.\n\nCMake gives a much looser staging analogy.",
  "m62": "CMake lets a project describe desired build structure abstractly, and a generator produces a concrete native buildsystem. That is a useful staging analogy for description followed by resolution. But compiler routes can carry legality, representation requirements, invalidation, and transformation meaning, so this is not simply “CMake for compilers.”\n\nThat leaves the research question I actually want to test.",
  "m63": "The candidate research question is this: can independently authored compiler and tooling components share an extensible semantic-contract layer across changing representations? Think typed and versioned schemas, shared query boundaries, and explicit validity — not one monolithic ontology and not one universal solver.\n\nThe important part is making this falsifiable.",
  "m64": "My primary experiment would start with existing consumers and one semantic producer, then add a second independent producer without modifying those consumers. Fresh, consistent evidence should be able to strengthen a valid judgement. But correctness comes first: stale or contradictory evidence must not discharge an obligation. Then we measure coupling, precision, invalidation cost, projection overhead, and explainability — with soundness as the first-order metric.\n\nAnd there is a very strong alternative that could make the whole shared substrate unnecessary.",
  "m65": "The strongest alternative may simply be local mechanisms: pass managers, IR interfaces, domain-specific analyses, local legalization, and explicit adapters. If those solve the real coupling problem with less machinery, they are the better architecture. A shared substrate is justified only if it measurably removes pairwise coupling without hiding domain semantics.\n\nAnd even if it does, the difficult questions remain.",
  "m66": "The open problems are not small details. How do we preserve identity across transformations? What evidence do we trust? How fine-grained can invalidation be without becoming too expensive? Which obligations belong in which planning or analysis engine? Those are exactly the questions that determine whether this architecture is useful or just elegant vocabulary.\n\nSo let me finish with the whole story in one sequence.",
  "m67": "Build the language from reusable capabilities. Resolve the ecosystem before execution. Freeze one concrete plan. Optimize globally wherever the resolved representations allow it. And as transformations erase high-level representation, keep the semantics that still matter queryable.\n\nCurrent UniversalToolchain gives us local evidence for staged composition, deabstraction, and cross-backend semantic parity. The broader semantic-contract layer is the experiment, not the conclusion. Thank you."
});
