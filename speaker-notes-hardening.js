Object.assign(window.SPEAKER_NOTES, {
  "m1": "ЗАЧЕМ: сразу дать один вопрос, один механизм и одну фразу для памяти, чтобы доклад не распадался на обзор API.\n\nСКАЗАТЬ: вопрос доклада — когда extensibility перестаёт быть просто configuration и становится planning problem? Ответ: когда независимо принадлежащие части начинают взаимодействовать между стадиями compiler pipeline, локальных деклараций уже недостаточно. Мы сначала собираем global decision, фиксируем его как data, а runtime затем материализует именно это решение. Формула на весь доклад: Declare locally. Resolve globally. Execute concretely.\n\nПЕРЕХОД: сначала честный baseline — никакого planner.\n\nНЕ ПЕРЕОБЕЩАТЬ: UniversalToolchain — case study и один design point, не универсальная замена другим toolchains.",
  "m2": "ЗАЧЕМ: убрать strawman и показать, что planner должен выигрывать у максимально простого решения, а не у искусственно плохой архитектуры.\n\nСКАЗАТЬ: если один owner уже знает parser, lowering, optimizers и backend, handwritten wiring проще, прозрачнее и обычно лучше. Здесь нечего «разрешать» глобально: владелец уже знает один конкретный pipeline. Planner имеет смысл только после появления failure case, который ручная цепочка начинает скрывать или централизовать в одном огромном integrator.\n\nПЕРЕХОД: такой failure появляется, когда продукт становится семейством concrete languages.\n\nНЕ ПЕРЕОБЕЩАТЬ: builder, DI и прямое связывание компонентов остаются хорошими решениями в своих областях.",
  "m3": "ЗАЧЕМ: объяснить мотивацию extensibility до введения внутренних классов UniversalToolchain.\n\nСКАЗАТЬ: shared infrastructure начинает обслуживать не один фиксированный язык, а несколько concrete configurations. Меняется feature surface, набор допустимых transformations, backend и runtime policy. Это не требует, чтобы каждый вариант был полностью отдельным compiler fork. Но чем больше стадий получают независимых owners, тем меньше шансов, что один local package способен честно выбрать весь executable pipeline.\n\nПЕРЕХОД: количество options само по себе ещё не делает planner необходимым.\n\nНЕ ПЕРЕОБЕЩАТЬ: language family здесь означает практический reuse одной инфраструктуры, а не формальную теорию наследования языков.",
  "m4": "ЗАЧЕМ: сформулировать точный threshold между configuration и planning.\n\nСКАЗАТЬ: независимые flags остаются configuration. Planning начинается не от количества параметров, а от constraints между ними: requires, conflicts, ordering, provider ambiguity и artifact reachability. В этот момент правильный ответ зависит от whole selected language. Локальный author знает свой contribution, но не имеет authority и информации выбирать всю цепочку. Поэтому нужен отдельный owner global decision.\n\nПЕРЕХОД: посмотрим, что именно остаётся локальным, а что отдаётся planner.\n\nНЕ ПЕРЕОБЕЩАТЬ: planner разрешает только явно выраженные constraints; он не угадывает intent и не доказывает корректность отсутствующих контрактов.",
  "m5": "ЗАЧЕМ: показать границу ответственности между integrator surface и extension contracts без превращения слайда в API tour.\n\nСКАЗАТЬ: integrator может выразить concrete language через маленький .wistdialect: feature set плюс backend. Это только input to planning. Отдельные extensions публикуют локальные факты: source/target artifact contracts, capabilities, ordering и planning cost. Важная архитектурная мысль: local extension описывает то, что знает локально, но не принимает whole-language решение за остальных. LanguageCompiler получает всю выбранную definition и registry.\n\nПЕРЕХОД: compiler-specific причина для planner становится видна, когда local transformations образуют graph.\n\nНЕ ПЕРЕОБЕЩАТЬ: textual order в dialect file не является runtime pipeline order и не должен подаваться как такой.",
  "m6": "ЗАЧЕМ: дать сильнейший compiler-specific proof, которого не хватает обычному примеру с двумя DI providers.\n\nСКАЗАТЬ: в fixed compiler мы рисуем линию Source→Syntax→Semantic→Bytecode→AIR→Backend. Но independent extensions могут добавлять альтернативные typed transformations, например Semantic→AIR. Теперь pipeline — это graph возможных representation transitions. Ни одна edge сама не знает, должна ли она попасть в итоговый route. Это и есть место, где whole-language planner становится содержательно compiler-specific.\n\nПЕРЕХОД: следующий вопрос — что именно planner гарантирует при выборе route.\n\nНЕ ПЕРЕОБЕЩАТЬ: наличие альтернативного edge само по себе не доказывает semantic equivalence двух путей.",
  "m7": "ЗАЧЕМ: точно отделить реальную текущую route-механику от соблазнительных, но неподтверждённых выводов.\n\nСКАЗАТЬ: current route phase строит type-compatible candidates, выбирает deterministic minimum planning cost и сохраняет ordered LanguageArtifactRoute. Это source-backed и test-backed behavior. Но structural compatibility не означает semantic equivalence, deterministic selection не означает correctness, а route Cost — протокольный planning weight, не measured latency. Эти три границы надо проговорить прямо на основном слайде, а не прятать в Q&A.\n\nПЕРЕХОД: после resolution route становится частью concrete data object.\n\nНЕ ПЕРЕОБЕЩАТЬ: не говорить «optimal compiler pipeline» без уточнения, что оптимальность только по выраженному planning cost.",
  "m8": "ЗАЧЕМ: сделать результат planning наблюдаемым и объяснить, почему это больше, чем transient DI wiring.\n\nСКАЗАТЬ: LanguagePlan хранит resolved features, contributions, exact runtime provider, routes, PlanHash и summary. Это inspectable decision record: global choices уже закрыты. Такой объект можно проверить, логировать, сравнивать и передавать runtime. PlanHash полезен как canonical identity выраженной конфигурации, но не превращает plan в proof of semantics или security.\n\nПЕРЕХОД: раз решение стало data, можно провести чёткую ownership boundary во времени.\n\nНЕ ПЕРЕОБЕЩАТЬ: immutable plan не означает, что все objects внутри runtime immutable или что execution не делает validation.",
  "m9": "ЗАЧЕМ: показать главный staging effect архитектуры: decision ownership отделён от repeated execution.\n\nСКАЗАТЬ: LanguageCompiler решает whole-language questions и производит LanguagePlan. LanguageRuntime.Create проверяет exact binding и материализует planned components. После этого Run выполняет requests в уже выбранной environment. Runtime validation остаётся; исчезает именно повторный global provider/route search. Это формулировка сильнее и честнее, чем «abstractions disappear» или «zero-cost extensibility».\n\nПЕРЕХОД: теперь покажем этот boundary на реальном source-backed demo.\n\nНЕ ПЕРЕОБЕЩАТЬ: no re-planning не равно no dispatch, no allocation, no validation и тем более не равно measured speedup.",
  "m10": "ЗАЧЕМ: дать executable evidence, не притворяясь, что текущий demo уже реализует идеальный route-changing сценарий.\n\nСКАЗАТЬ: текущий safe demo сначала показывает реальную UTL2002 ambiguity и explicit PreferCapabilityProvider. Затем он строит concrete LanguagePlan с route, LanguageRuntime.Create materializes exact plan, и synthetic backend превращает 41 в 42. Этот demo слабее будущего сценария, где новая transformation edge меняет selected route, поэтому не используем provider ambiguity как единственный аргумент в пользу planner — route уже доказан предыдущими слайдами и тестами.\n\nПЕРЕХОД: после demo важно развести два разных смысла слова once.\n\nНЕ ПЕРЕОБЕЩАТЬ: demo не доказывает Wist semantics, semantic route equivalence, performance или production maturity ecosystem.",
  "m11": "ЗАЧЕМ: снять потенциально опасную двусмысленность вокруг фразы «planning happens once».\n\nСКАЗАТЬ: есть environment boundary и program boundary. WistEngine.Create выбирает definition, строит LanguagePlan и LanguageRuntime один раз для конкретного engine instance. Отдельно Compile<TDelegate> строит program artifact и durable delegate для повторного вызова. Эти две операции нельзя смешивать: reuse environment не означает reuse compiled program. Такой staging особенно важен для честного разговора о cost model и API semantics.\n\nПЕРЕХОД: поэтому отдельно разберём Evaluate, который часто хочется ошибочно назвать compile-once path.\n\nНЕ ПЕРЕОБЕЩАТЬ: не утверждать, что все public operations thread-safe или что engine lifetime автоматически соответствует deployment lifetime.",
  "m12": "ЗАЧЕМ: заранее закрыть вопрос аудитории «если planning once, значит Evaluate уже compiled once?»\n\nСКАЗАТЬ: нет. Evaluate(code) переиспользует выбранную environment, но отправляет source request в Runtime.Run. Explicit reusable-program boundary — Compile<TDelegate>, где Runtime.Build создаёт artifact, затем materialization создаёт durable delegate. Это очень полезный пример того, почему architectural staging нужно описывать через конкретные boundaries, а не маркетинговые слова вроде cached/compiled.\n\nПЕРЕХОД: теперь можно честно разложить performance cost по стадиям.\n\nНЕ ПЕРЕОБЕЩАТЬ: не делать вывод о сравнительной скорости Evaluate и Compile без benchmark artifact.",
  "m13": "ЗАЧЕМ: ответить на неизбежный performance challenge без придуманных чисел.\n\nСКАЗАТЬ: архитектура позволяет утверждать только staging fact: whole-language composition не обязана rediscover providers/routes на каждом execution. Отдельно существуют planning cost, runtime creation, cold first execution и steady-state execution. Interfaces, allocations, dispatch, JIT и backend startup могут оставаться. Поэтому на слайде единственный performance status — NEEDS MEASUREMENT, а не «fast» или «zero overhead».\n\nПЕРЕХОД: если planner имеет собственную стоимость, когда он вообще оправдан?\n\nНЕ ПЕРЕОБЕЩАТЬ: route Cost не использовать как latency proxy; никаких чисел без raw benchmark evidence.",
  "m14": "ЗАЧЕМ: закончить архитектурную часть практическим выбором механизма, а не призывом использовать UniversalToolchain везде.\n\nСКАЗАТЬ: stable pipeline — handwritten wiring. Independent options — builder/configuration. Object lifetime/substitution — DI. Compiler pass execution — pass manager. IR legalization — dialect conversion. Whole-language planner появляется только когда independently owned language choices взаимодействуют между стадиями и ни один local owner не может честно выбрать весь executable route. Эта таблица защищает доклад от architecture astronautics.\n\nПЕРЕХОД: финал сжимает всё до трёх глаголов.\n\nНЕ ПЕРЕОБЕЩАТЬ: не заявлять novelty или superiority относительно LLVM, MLIR, MPS, Racket, DI или builders.",
  "m15": "ЗАЧЕМ: оставить аудитории одну переносимую mental model без class-name dependency.\n\nСКАЗАТЬ: Declare locally — extensions описывают local facts and transformations. Resolve globally — planner видит interacting choices, выбирает concrete language и one route, фиксирует ответ как immutable plan. Execute concretely — runtime materializes exactly that decision. И анти-takeaway: если один owner уже знает stable pipeline, planner не нужен — wire it by hand. Это вся история доклада в тридцати секундах.\n\nПЕРЕХОД: Q&A; appendix содержит evidence map, prior-art boundaries, security, performance measurement и compiler optimization details, убранные из main narrative.\n\nНЕ ПЕРЕОБЕЩАТЬ: не возвращать в устный финал CSE, SSA, intrinsics или EGraph — они снова размоют одну главную идею."
});

function formatSpeakerNote(note) {
  return String(note || '')
    .replace(/\s+(?=(?:ЗАЧЕМ|СКАЗАТЬ|ПЕРЕХОД|ДЕТАЛЬ|НЕ ПЕРЕОБЕЩАТЬ):)/g, '\n\n')
    .trim();
}

for (const key of Object.keys(window.SPEAKER_NOTES)) {
  window.SPEAKER_NOTES[key] = formatSpeakerNote(window.SPEAKER_NOTES[key]);
}

for (const slide of document.querySelectorAll('.slide[data-note-key]')) {
  slide.dataset.notes = window.SPEAKER_NOTES[slide.dataset.noteKey] || '';
}
