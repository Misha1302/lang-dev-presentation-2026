Object.assign(window.SPEAKER_NOTES, {
  "m1": "ЗАЧЕМ: дать одну причинную модель на весь доклад, а не обещать набор compiler features.\n\nСКАЗАТЬ: у истории три шага: Resolve globally — whole-language choices превращаются в один LanguagePlan; Justify locally — отдельный compiler pass меняет IR только когда у него есть явные semantic и capability evidence; Execute concretely — runtime работает с уже выбранной средой. Эта формула связывает extensibility и compiler reasoning в одну историю.\n\nПЕРЕХОД: сначала проверим сильнейший baseline — обычный pipeline руками.\n\nНЕ ПЕРЕОБЕЩАТЬ: не утверждать zero-cost, semantic proof или performance win.",
  "m2": "ЗАЧЕМ: убрать strawman и показать decision rule с самого начала.\n\nСКАЗАТЬ: если один owner заранее знает parser, lowering, optimizer и backend, явный handwritten pipeline проще и прозрачнее. Planner — не награда за архитектурность; он должен оправдываться failure case, который возникает из независимых компонентов и whole-language choices.\n\nПЕРЕХОД: такой failure появляется, когда продукт — уже не один фиксированный язык, а family.\n\nНЕ ПЕРЕОБЕЩАТЬ: DI, builder и ручная композиция не объявляются устаревшими.",
  "m3": "ЗАЧЕМ: объяснить практический мотив extensibility до появления planner vocabulary.\n\nСКАЗАТЬ: текущий Wist использует общую инфраструктуру для нескольких concrete definitions: minimal arithmetic, restricted pricing, full default и других presets. Мы не обязаны форкать весь compiler для каждого профиля; интегратор выбирает нужную поверхность, backend и policies.\n\nПЕРЕХОД: пока эти choices независимы, это всё ещё обычная configuration problem.\n\nНЕ ПЕРЕОБЕЩАТЬ: не называть эти variants semantic inheritance или доказательством универсальности.",
  "m4": "ЗАЧЕМ: показать, что options — нормальный дизайн до определённого порога.\n\nСКАЗАТЬ: feature set, backend, optimizer и policies могут выглядеть как флаги. Пока выбор каждого пункта не зависит от остальных, builder вполне достаточен. Архитектурный перелом не в количестве флагов, а в появлении constraints между ними.\n\nПЕРЕХОД: следующий слайд показывает этот перелом.\n\nНЕ ПЕРЕОБЕЩАТЬ: не считать любое большое configuration API planner-worthy.",
  "m5": "ЗАЧЕМ: сформулировать критерий, когда configuration становится planning.\n\nСКАЗАТЬ: feature может требовать feature; capability может иметь несколько providers; contributions могут конфликтовать и требовать ordering; backend требует artifact route; runtime должен поддержать выбранный plan. Теперь правильный ответ зависит от whole selected language, а локальный package author всей картины не видит.\n\nПЕРЕХОД: значит, нужен owner, который имеет право видеть всю конфигурацию.\n\nНЕ ПЕРЕОБЕЩАТЬ: planner разрешает только выраженные constraints, а не намерения автора.",
  "m6": "ЗАЧЕМ: разделить локальное объявление facts и глобальное принятие решения.\n\nСКАЗАТЬ: package author знает локальные requirements/provisions; language integrator выбирает definition и policy; LanguageCompiler получает whole definition + registry и выполняет resolution. Именно ownership, а не DI container как таковой, является центральной архитектурной темой.\n\nПЕРЕХОД: проверим это на prediction — два локально корректных provider.\n\nНЕ ПЕРЕОБЕЩАТЬ: роли conceptual; один человек может выполнять несколько ролей.",
  "m7": "ЗАЧЕМ: вовлечь аудиторию коротким prediction→reveal и показать реальный global failure.\n\nСКАЗАТЬ: спросить «Two providers are valid. Who wins?» Дать секунду. Ответ: никто молча. Current LanguageCompiler возвращает UTL2002, если capability имеет несколько eligible providers без explicit preference. Затем одна строка PreferCapabilityProvider превращает ambiguity в явную language-level policy.\n\nПЕРЕХОД: provider choice — только один global decision; backend ещё должен быть достижим по artifact route.\n\nНЕ ПЕРЕОБЕЩАТЬ: planner не знает, какой provider семантически “лучше”; integrator делает policy explicit.",
  "m8": "ЗАЧЕМ: показать второй класс whole-language reasoning без введения лишнего compiler theory.\n\nСКАЗАТЬ: transformer объявляет source/target contracts, backend — input contract. Planner видит весь граф и выбирает type-compatible route, который записывается в LanguagePlan как ordered steps и TotalCost. Route cost — protocol/planning weight, а не benchmark latency.\n\nПЕРЕХОД: после provider и route resolution можно показать, что именно стало concrete data.\n\nНЕ ПЕРЕОБЕЩАТЬ: structural contract не означает semantic equivalence.",
  "m9": "ЗАЧЕМ: сделать LanguagePlan наблюдаемым продуктом resolution, а не абстрактной коробкой.\n\nСКАЗАТЬ: plan содержит resolved Features, Contributions, RuntimeProvider, Routes, PlanHash и Summary. Это уже не instructions «выбери что-нибудь потом»; это snapshot concrete whole-language environment. Он пригоден для verification, drift detection и exact materialization.\n\nПЕРЕХОД: теперь staging boundary во времени становится очевидной.\n\nНЕ ПЕРЕОБЕЩАТЬ: PlanHash — canonical identity выраженной конфигурации, не semantic/security proof.",
  "m10": "ЗАЧЕМ: показать, где заканчивается global planning и начинается repeated execution.\n\nСКАЗАТЬ: authoring оставляет possibilities открытыми; composition создаёт LanguagePlan; LanguageRuntime.Create verifies exact binding и materializes planned components; Run работает через созданную session. Runtime validation остаётся, но второй global provider/route search не запускается.\n\nПЕРЕХОД: теперь это можно увидеть на коротком executable demo.\n\nНЕ ПЕРЕОБЕЩАТЬ: отсутствие re-planning не означает отсутствие runtime work.",
  "m11": "ЗАЧЕМ: доказать staging boundary реальным executable path и одновременно убрать прежнее несоответствие slide↔source.\n\nСКАЗАТЬ: demo намеренно synthetic. Сначала Demo.Ambiguity выдаёт UTL2002, затем explicit provider preference даёт plan. Потом Demo.Runtime строит tiny route SourceText→int, selected backend возвращает value+1, LanguageRuntime.Create materializes exact plan, а Run(\"41\") даёт 42. Показываем максимум три observable state changes: failure, resolved plan summary, execution result.\n\nПЕРЕХОД: planning доказал, что мы знаем, WHAT EXISTS. Но это ещё не отвечает, WHAT MAY CHANGE внутри compiler IR.\n\nНЕ ПЕРЕОБЕЩАТЬ: это не Wist semantics demo, не benchmark и не доказательство semantic compatibility.",
  "m12": "ЗАЧЕМ: построить критический мост, чтобы compiler section не выглядел второй презентацией.\n\nСКАЗАТЬ: LanguagePlan отвечает глобально «какая конкретная environment выбрана»: providers, routes, runtime, backend. Но optimizer решает другую задачу: «можно ли здесь законно изменить program representation?» Для этого нужны local semantic facts и capabilities выбранной среды. Planner не производит автоматически все semantic guarantees.\n\nПЕРЕХОД: сначала semantic evidence — один маленький CSE example.\n\nНЕ ПЕРЕОБЕЩАТЬ: global composition evidence и local semantic evidence — разные источники истины.",
  "m13": "ЗАЧЕМ: показать transferable compiler insight без обучения CSE или SSA.\n\nСКАЗАТЬ: спросить аудиторию: «Can these two foo(x) calls be merged?» Имя foo ничего не доказывает. Current local CSE и constant folding требуют CallableDescriptor с pure effects, deterministic behavior и trusted level; неизвестные/effectful/nondeterministic/untrusted calls остаются. Поэтому architecture переносит legality из operation-name folklore в explicit semantic properties.\n\nПЕРЕХОД: semantic evidence отвечает, что transformation допустима по смыслу; но emitted representation ещё должна поддерживаться выбранным execution environment.\n\nНЕ ПЕРЕОБЕЩАТЬ: descriptor metadata само по себе не доказывает, что metadata истинно.",
  "m14": "ЗАЧЕМ: связать LanguagePlan напрямую с compiler specialization через реально connected path.\n\nСКАЗАТЬ: спросить «Can optimizer emit specialization if selected backend cannot consume it?» Current Wist path строит capability context из actual selected backend capabilities и plan intrinsic policy. Supports(...) скрывает forbidden/unsupported intrinsic from optimizer. После optimization backend снова Validate(...) emitted AIR и fails closed. Это реальная end-to-end связь между global plan и local rewrite legality.\n\nПЕРЕХОД: теперь обе половины можно собрать в одну архитектуру на двух scales.\n\nНЕ ПЕРЕОБЕЩАТЬ: это current Wist typed-intrinsic path, не универсальная гарантия для любого optimizer/backend.",
  "m15": "ЗАЧЕМ: закрепить одну причинную картинку и сразу провести четыре truth boundaries.\n\nСКАЗАТЬ: global layer отвечает за concrete environment; local layer — за конкретный rewrite. Они связаны, но не подменяют друг друга. Plan structure не доказывает semantics. Semantic metadata не доказывает собственную правдивость. Capability availability не является performance claim. Restricted composition не является sandbox.\n\nПЕРЕХОД: финальный слайд превращает всё в практический decision rule.\n\nНЕ ПЕРЕОБЕЩАТЬ: не превращать layered evidence в theorem о correctness всей language implementation.",
  "m16": "ЗАЧЕМ: оставить одну фразу, одну картинку и один decision rule на неделю после доклада.\n\nСКАЗАТЬ: Resolve globally. Justify locally. Execute concretely. Если один owner знает весь pipeline — wire it by hand. Если independent pieces создают whole-language choices — resolve them once into a plan. Если optimizer хочет переписать IR — demand explicit semantic and capability evidence. Это и есть вся история доклада.\n\nПЕРЕХОД: Q&A; appendix содержит evidence, performance boundary, IR contracts и attack surfaces.\n\nНЕ ПЕРЕОБЕЩАТЬ: UniversalToolchain — один design point, не универсальная замена DI, LLVM/MLIR или handwritten toolchains."
});

function formatSpeakerNote(note) {
  return String(note || '')
    .replace(/\s+(?=(?:ЗАЧЕМ|СКАЗАТЬ|ПЕРЕХОД|ДЕТАЛЬ|НЕ ПЕРЕОБЕЩАТЬ):)/g, '\n\n')
    .trim();
}

for (const key of Object.keys(window.SPEAKER_NOTES)) {
  window.SPEAKER_NOTES[key] = formatSpeakerNote(window.SPEAKER_NOTES[key]);
}

for (const slide of document.querySelectorAll('.slide[data-note-key]')) {
  slide.dataset.notes = window.SPEAKER_NOTES[slide.dataset.noteKey] || '';
}
