Object.assign(window.SPEAKER_NOTES,{
  "m9":"ЗАЧЕМ: дать строгую формулировку deabstraction, чтобы название доклада не звучало как обещание zero-cost abstractions. СКАЗАТЬ: до planning provider, route, contribution order и runtime остаются множествами кандидатов или частично заданными constraints. После LanguageCompiler.Compile эти глобальные choices представлены конкретными данными LanguagePlan: exact provider identity, exact route steps, deterministic ordered contributions, runtime provider/policy. Вот что исчезает — необходимость снова принимать эти глобальные решения. Interfaces, plan object, collections, validation и dispatch могут физически остаться. Поэтому deabstraction здесь прежде всего staging/ownership transformation: open choice превращается в resolved data. Это позволяет inspect/hash/test composition отдельно от execution. ПЕРЕХОД: теперь разделю guaranteed composition deabstraction и возможную дальнейшую specialization, чтобы не смешивать architectural staging с optimizer claim. НЕ ПЕРЕОБЕЩАТЬ: deterministic plan не означает semantic equivalence выбранных extensions и не означает zero runtime overhead.",
  "m10":"ЗАЧЕМ: развести два разных смысла слова deabstraction. СКАЗАТЬ: слой 1 уже реализован и является architectural guarantee — planner схлопывает candidate sets и constraints в concrete identities, routes и order; runtime получает resolved plan и не должен повторять global composition. Слой 2 — optional representation/code specialization: если slot, type, provider или route уже известны, конкретная implementation/JIT/AOT потенциально может убрать wrappers, lookup или часть indirect dispatch. Но это отдельная optimization problem и отдельный benchmark claim. Нельзя из факта planning автоматически выводить devirtualization или faster generated code. Правильная формула: planning removes dynamic choice from the composition boundary; specialization may remove some representation/dispatch cost later. ПЕРЕХОД: после определения границы показываю runnable evidence того, что layer 1 действительно существует в current source: planner failure, corrected plan и exact runtime execution. НЕ ПЕРЕОБЕЩАТЬ: второй слой не является current universal guarantee.",
  "m14":"ЗАЧЕМ: показать не абстрактное 'framework has overhead', а карту стоимости. СКАЗАТЬ: цена распределяется минимум по четырём местам. Первое — one-time planning latency/allocations: provider resolution, ordering, conflict checks, route search, canonicalization, diagnostics. Второе — plan/runtime creation: память immutable plan, verification, materialization конкретных components и route assembly. Третье — steady-state execution: по design здесь не должно быть re-planning, но обычные provider/session calls, validation и dispatch всё ещё могут стоить время. Четвёртое — engineering cost: protocol/versioning/documentation, determinism regressions и configuration state space для tests. Поэтому честный performance analysis не сводится к одному числу. Нужно отдельно мерить planning, runtime creation, first execution и steady state, а затем сравнивать с handwritten baseline. ПЕРЕХОД: после стоимости ставлю ещё более важную границу — даже структурно valid plan не доказывает semantic compatibility. НЕ ПЕРЕОБЕЩАТЬ: перенос решения из hot path не доказывает, что total cost меньше для любого workload.",
  "a3":"Appendix note: на вопрос о performance отвечать четырьмя отдельными величинами: planning, runtime creation, first execution и steady-state execution. Для planning полезны synthetic scales 10/100/1000 contributions и time+allocations; runtime creation измеряет verification/materialization/route assembly; first execution отделяет initialization/JIT/warmup; steady state проверяет, что runtime не re-plan-ит и измеряет оставшийся execution overhead. В current evidence есть benchmark/perf/allocation gates, но из доступного evidence нет talk-ready набора универсальных чисел и нет честного handwritten-baseline speedup result. Поэтому на слайде стоит NEEDS MEASUREMENT, а не выдуманные nanoseconds. Если появятся воспроизводимые numbers, они должны быть привязаны к exact commit, SDK, OS, CPU, command и raw artifacts."
});
for(const slide of document.querySelectorAll('.slide[data-note-key]')){
  slide.dataset.notes=window.SPEAKER_NOTES[slide.dataset.noteKey]||'';
}
