Object.assign(window.SPEAKER_NOTES,{
  "m1":"ЗАЧЕМ: сразу зафиксировать новый architectural thesis без обещания магически бесплатной extensibility. СКАЗАТЬ: extensions описывают possibilities локально, LanguageCompiler видит whole-language configuration и один раз разрешает global composition choices в concrete LanguagePlan, а LanguageRuntime materializes и executes именно этот resolved plan. Поэтому название Build an Extensible Language, Run a Concrete One говорит о смене стадии принятия решений: extensible world остаётся до planning, concrete world начинается после него. Performance consequence узкий: repeated execution не обязано заново выбирать provider, route, runtime и ordering. Это не performance talk и не утверждение, что interfaces, objects или dispatch исчезли. ПЕРЕХОД: прежде чем оправдывать planner, покажу strongest baseline — ручной pipeline, который для фиксированного языка часто лучше. НЕ ПЕРЕОБЕЩАТЬ: не обещать speedup, free abstraction, devirtualization или равенство handwritten C#.",
  "m7":"ЗАЧЕМ: показать composition deabstraction на данных, а не на метафоре. СКАЗАТЬ: до planning provider может быть A или B, route — R1/R2/R3, runtime — X/Y, а ordering задан partial constraints. После LanguageCompiler.Compile эти choices представлены concrete fields LanguagePlan: resolved contribution identities, exact route steps, exact runtime provider и deterministic contribution order. Поэтому корректная формула — open composition choices become concrete plan data. Abstraction исчезает как нерешённый вопрос; object model при этом может остаться. PlanHash добавляет reproducible identity этой resolved representation, но не делает её semantic proof. ПЕРЕХОД: если план уже хранит глобальные решения, нужно провести строгую границу — кто имеет право снова их открывать. НЕ ПЕРЕОБЕЩАТЬ: route Cost — protocol metric выбора маршрута, а не измеренное execution time и не доказательство глобально оптимальной конфигурации.",
  "m8":"ЗАЧЕМ: доказать planner/runtime boundary current source. СКАЗАТЬ: LanguageCompiler.Compile выполняет feature/contribution resolution и LanguageArtifactRoutePhase, затем создаёт LanguagePlan. LanguageRuntime.Create принимает уже готовый plan, вызывает verifier, проверяет exact provider ID/version, contribution identity, Toolchain API, selected routes и policy, materializes planned components и создаёт session. Run проверяет вход относительно plan и вызывает session; он не вызывает LanguageCompiler и не ищет новый global route/provider. Runtime validation здесь обязательна, но validation exact binding не равна second planning pass. ExactRuntimeBindingTests дополнительно проверяют отказ для equivalent-but-different registration и materialization only planned components. ПЕРЕХОД: из этой границы следует небольшой performance payoff: extensibility не обязана оставаться dynamic composition problem внутри repeated execution. НЕ ПЕРЕОБЕЩАТЬ: runtime creation и каждый Run всё ещё могут иметь validation, dispatch и allocation costs.",
  "m9":"ЗАЧЕМ: сформулировать главный performance-adjacent payoff за несколько секунд. СКАЗАТЬ: extensible at composition time, concrete at execution time. Слева packages, providers, routes, ordering и conflicts — пространство возможностей. Planning once превращает его в LanguagePlan. Справа runtime получает exact provider, exact route, exact runtime и exact ordering и исполняет resolved choices вместо их повторного открытия. На вопрос «так extensibility бесплатна?» ответ: нет. Planning, materialization и оставшиеся runtime abstractions стоят ресурсов. Architectural claim уже: global composition decisions не обязаны быть dynamic на каждом Run. Их remaining hot-path cost — отдельный empirical question. ПЕРЕХОД: теперь отделю гарантированную composition deabstraction от возможной representation/code specialization. НЕ ПЕРЕОБЕЩАТЬ: отсутствие re-planning не означает отсутствие dispatch, validation, allocations или иных runtime overheads.",
  "m10":"ЗАЧЕМ: не дать слову deabstraction превратиться в обещание optimizer-а. СКАЗАТЬ: layer 1 — current guarantee. Candidate providers, routes, runtime and order становятся concrete LanguagePlan data до execution, и Run не повторяет global composition. Layer 2 — отдельная optimization problem: known types/providers/routes потенциально могут позволить JIT/AOT убрать lookup, wrapper или indirect call, но current architecture этого универсально не гарантирует. Interfaces и objects могут остаться, runtime verification может остаться, dispatch может остаться. На вопрос «вы утверждаете zero-cost abstractions?» ответ: нет; я утверждаю только, что composition choices становятся concrete до execution. ПЕРЕХОД: дальше показываю source-backed demo именно layer 1, не speculative specialization. НЕ ПЕРЕОБЕЩАТЬ: planning therefore devirtualization — неверный вывод без отдельного implementation evidence и benchmark.",
  "m14":"ЗАЧЕМ: заменить vague framework-overhead discussion явной четырёхстадийной cost model. СКАЗАТЬ: первое — planning: resolution, conflicts, ordering, route search, canonicalization, diagnostics, allocations. Второе — runtime creation: verification, materialization, route/session assembly. Третье — first execution: parsing/lowering/codegen где применимо, initialization, JIT, cold caches. Четвёртое — steady state: selected path уже concrete и global re-planning по architecture нет, но provider/session calls, validation, dispatch, allocations и generated-code quality всё ещё могут определять результат. Плюс остаётся engineering cost protocol/versioning/testing. Концептуальная amortization модель: naive N × (Ccomposition + Cexecution), staged Ccomposition + N × Cexecution; доля Ccomposition/N падает с N. Это decomposition модели, не benchmark и не доказательство, что UniversalToolchain быстрее альтернативы. На вопрос «тогда почему это должно быть быстрее?» ответ: architecture не гарантирует faster final program; она гарантирует лишь, что repeated execution не обязано повторно решать global composition. ПЕРЕХОД: после стоимости ещё раз ограничу доказательство — structural compatibility не равна semantics. НЕ ПЕРЕОБЕЩАТЬ: no re-planning не равно zero overhead и planning не обязано окупаться для любого workload.",
  "m16":"ЗАЧЕМ: закончить одной переносимой архитектурной формулой. СКАЗАТЬ: extensions describe possibilities; planning resolves global composition choices into one LanguagePlan; runtime executes the resolved language. Отсюда takeaway: be extensible when composing, be concrete when executing. Для одного заранее известного pipeline manual composition остаётся сильнейшим baseline; planner нужен только там, где независимые extensions создают whole-system decisions. Performance consequence вторичен и узок: runtime should not repeatedly pay for decisions already made. Всё остальное — remaining validation, dispatch, allocations, generated-code quality — измеряется отдельно. ПЕРЕХОД: в Q&A можно открыть appendix с current-source evidence, demo contract и четырьмя measurement boundaries. НЕ ПЕРЕОБЕЩАТЬ: финал не утверждает, что extensibility всегда быстрее, что abstractions физически исчезают или что planning доказывает semantic correctness.",
  "a3":"Appendix note: на вопрос «Won't extensibility hurt performance?» разделить architecture и measurement. Architecture result: current LanguageCompiler resolves global composition into LanguagePlan, а repeated LanguageRuntime.Run не запускает LanguageCompiler/route search/provider selection заново. Empirical result: размер planning, runtime creation, first-execution и steady-state costs требует exact-revision measurement. Current source содержит MigrationArchitectureBoundaryBenchmarks для LanguagePlan_Compile/WistEngine_CreateAndDispose, FormulaHotPathBenchmarks с prepared C# и Wist delegates и parity check, FormulaCompilationBenchmarks и FormulaConvenienceBenchmarks. Convenience Evaluate нельзя сравнивать с prepared delegate как будто это один workload. Публичных talk-ready raw numbers для current 7005371d revision здесь не зафиксировано, поэтому все четыре строки честно остаются NEEDS MEASUREMENT. Формула Ccomposition + N × Cexecution — conceptual amortization model, не speedup evidence."
});
for(const slide of document.querySelectorAll('.slide[data-note-key]')){
  slide.dataset.notes=window.SPEAKER_NOTES[slide.dataset.noteKey]||'';
}
