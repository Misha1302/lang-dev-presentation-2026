window.SPEAKER_SCRIPT = Object.freeze({
  ...window.SPEAKER_SCRIPT,
  "r1": "Before going further, I wanted to check whether this problem is common enough to care about.\n\nThere is a large empirical study of Xtext projects that found hundreds of developed languages and studied how their grammars evolve over time.\n\nSo languages do change. The more useful question is economic: when does repeated variation become expensive enough that explicit reuse is worth the extra machinery?",
  "r2": "And reuse is not a free win.\n\nThe classic trade-off is simple. Clone-and-own is cheap at the beginning: copy something, change it, move on. A reusable platform costs more up front because you have to design the shared structure first.\n\nThe bet is that repeated variants become cheaper later. But that is only a bet. Propagating a change through a shared system is not automatically cheaper, so we tested it instead of assuming it.",
  "r3": "This is the small experiment we used.\n\nWe started with one pricing language, then created a discount variant, a surcharge variant, and a combined variant. We implemented the same workload with clone-and-own and with UniversalToolchain composition.\n\nThe important part is that we froze the changes and the correctness oracle before looking at the result. A negative result was allowed. We wanted to find where the extra composition machinery actually starts paying for itself.",
  "r4": "The result was mixed, which is actually useful.\n\nFor the second variant, the new feature itself was smaller with UniversalToolchain: 19 lines versus 35 in the clone-and-own version.\n\nBut if we count the whole treatment so far, UniversalToolchain was still larger: 105 lines versus 61.\n\nSo we reached a marginal crossover before a total crossover. One new variant became cheaper, but the platform had not yet paid back its initial cost.",
  "r5": "We also tested a change that we expected to favor composition, and it did not.\n\nThe change was a shared percentage-range rule. In both implementations, the fair solution changed one logical place, one file, and seven lines.\n\nWhy? Because the clone baseline could use an ordinary shared utility.\n\nSo this expected advantage disappeared. That is a useful warning: not every shared change is evidence that we need language-level composition.",
  "r6": "We saw the same thing in the downstream pipeline.\n\nA normal shared pipeline handled this particular change in one place, and UniversalToolchain also handled it in one place. Neither approach had to touch the variant-specific semantics.\n\nThat means shared IR and shared compiler stages are valuable, but they are not the same thing as an extensible source language.\n\nSo we should not use ordinary downstream reuse as evidence for the stronger architecture.",
  "r7": "That first experiment is finished, and its result is deliberately limited.\n\nIt tells us something about the economics of reuse: upfront cost, marginal variant cost, duplication, and propagation.\n\nThe semantic-evidence idea in the second half of the talk is a different question. Experiment one does not prove it.\n\nSo from here on, I treat the evidence-lifecycle model as a new hypothesis that needs its own experiment."
});
