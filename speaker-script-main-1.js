Object.assign(window.SPEAKER_NOTES, {
  "m1": "Hi. I want to talk about a compiler architecture question: can we build a language from independently authored components, but still end up with one concrete compiler that can be reasoned about and optimized as a whole?\n\nThe phrase “make the abstractions disappear” is not a zero-overhead claim. It means that composition uncertainty should disappear once planning is finished.\n\nSo first, why would we want this kind of extensibility at all?",
  "m2": "A language usually starts small: arithmetic, then variables, scopes, types, arrays, functions, effects, maybe several backends. Every addition expands more than syntax. It expands the semantic surface that the compiler has to keep coherent.\n\nAnd that surface tends to cross compiler layers.",
  "m3": "One feature may touch parsing, the AST, type checking, analyses, IR, lowering, optimization, backends, and tooling. Not every feature touches every layer, of course. The point is simply that monolithic evolution creates repeated cross-layer coupling pressure.\n\nBut that does not mean a monolithic compiler is bad.",
  "m4": "If you have one language, one owner, known targets, and a stable roadmap, a normal monolithic compiler is often exactly the right engineering choice. Extensibility is not automatically better. It is worth paying for only when reuse, variation, or independent development matters enough.\n\nThe situation changes when one ecosystem needs to produce several languages.",
  "m5": "Imagine the same ecosystem producing a tiny calculator, a restricted pricing DSL, and a more general language profile with loops, interop, and multiple backends. At that point, repeatedly forking or editing one monolith becomes a less useful unit of work.\n\nThat suggests a different unit: reusable language capabilities.",
  "m6": "So the question becomes: what if arithmetic, scopes, types, arrays, calls, optimizers, and backends could be reusable building blocks, rather than permanent edits to one fixed compiler? This is more than just loading plugins. We want reusable pieces of a language.\n\nWhat do we actually gain from that?",
  "m7": "We gain four useful options: reuse infrastructure across profiles, experiment by swapping implementations, let independent authors contribute extensions, and express policy by building restricted or target-specific languages. Those are real benefits — but they create a new problem.\n\nComposition itself becomes a systems problem.",
  "m8": "Once components are independent, somebody has to resolve dependencies, conflicts, providers, ordering, representations, backends, and policy. Extensibility does not remove complexity. It moves complexity from repeated manual integration into an explicit composition system. This arrow chain is what the first half of the talk is about.\n\nLet me first define the pieces in the current system.",
  "m9": "In the current Wist implementation I use three related terms. A Wist module alias is a selectable Wist-facing unit, such as Arithmetic or Scopes. A Feature is the generic capability requested by a language definition. A Contribution is an implementation participant: syntax ownership, a pass, a conversion, a backend, tooling, and so on. Optimizers and backends are ecosystem component categories too; they are not all Wist modules.\n\nWith that vocabulary, independent authorship becomes easier to state precisely.",
  "m10": "Suppose one developer authors a type or refinement feature, while another authors an array bounds feature. The first modularity goal is boring but important: both should be able to join the ecosystem without coordinating through private APIs.\n\nThen the same component ecosystem can produce several language profiles.",
  "m11": "Here the same ecosystem produces a tiny arithmetic profile, a restricted pricing profile, and a fuller language. The profiles choose different capabilities and backends, but reuse the same component pool. This is the core mental model: we are assembling a language, not merely configuring one fixed pipeline.\n\nIn Wist, that profile is called a dialect — with one important terminology warning.",
  "m12": "In this talk, a Wist dialect is a language profile or configuration. It selects modules, optimizers, policy, and backends. This is not the MLIR meaning of dialect. I will mention MLIR later, but only as prior art and analogy.\n\nNow we can look at real configurations.",
  "m13": "This is a real MinimalArithmetic profile. It requests Arithmetic, Numbers, Scopes, and Whitespaces, and chooses the interpreter backend. The syntax on the slide is executable configuration, not pseudocode. One detail matters: these are the aliases written by the user; dependency closure may add required features or contributions to the final plan.\n\nThe same configuration language scales to a much larger profile.",
  "m14": "Here is FullDefault. It requests a much broader language surface, two backends, optimizer features, and trusted interop policy. The important point is not the specific list. The user still declares the language they want. They are not manually spelling a pass-by-pass compiler pipeline.\n\nAnd configuration can remove capabilities as well as add them.",
  "m15": "PricingRestricted deliberately excludes capabilities and enables restricted policy. In the current implementation, `exclude` is a fail-closed constraint on this definition and its dependency closure; it is not subtraction from some inherited base dialect. And “restricted” here is composition and interop policy — it is not a process sandbox.\n\nSo the configuration has a very clear responsibility.",
  "m16": "The DSL says what language, backend, optimizer intent, and policy we want. The planner answers how to assemble that request: which dependencies, providers, ordering constraints, and executable routes form one admissible compiler configuration.\n\nNow let us follow that declaration into the compiler.",
  "m17": "At this point we can choose a language profile. The next problem is turning that choice into one concrete compiler.\n\nFirst, a simple compiler mental model.",
  "m18": "As a teaching model, think of source moving through syntax, semantic representation, lowering, optimization, and a backend. Real compilers may interleave or repeat these stages, so this is not a universal phase law. It is just enough structure for the composition problem.\n\nNow map that onto Wist.",
  "m19": "Wist currently has a concrete sequence through lexer and parser, AST, Bytecode, AIR, optimizers, and then interpreter or CIL execution. This is current implementation evidence. It is not a claim that every language in the architecture must use Bytecode and AIR.\n\nThat distinction is important enough to make explicit.",
  "m20": "Bytecode and AIR are Wist choices. Another language could use bound trees, SSA, graph IRs, executable artifacts, or something else. The reusable architectural idea is not one privileged representation stack.\n\nSo what is the generic planning flow?",
  "m21": "A `.wistdialect` is compiled into a LanguageDefinition: the requested language. The package registry provides the available typed components. A third-party package enters the same path explicitly: it contributes typed Feature and Contribution descriptors plus implementations, and the host adds it with `AddPackage`. LanguageCompiler does not hard-code that package; it resolves the selected descriptors into one LanguagePlan. Discovery and authoring feed inputs into the planner; they do not become a second runtime planner.\n\nThat gives us a clean staging boundary.",
  "m22": "I like to summarize the current staging as four nouns. Definition is the request. Compiler performs resolution. Plan is the immutable answer. Runtime materializes and executes that answer. This separation becomes important once planning is no longer just a linear list.\n\nBecause route alternatives turn part of the composition problem into a graph.",
  "m23": "This graph is intentionally narrower than a global compiler graph. At this point features, providers, and contributions are already resolved. For one enabled backend, nodes are artifact contracts and edges are transformation contributions. The selected path becomes that backend's route in LanguagePlan. Route reachability does not choose a provider or choose which backend exists.\n\nNow we can place the other structural constraints around that route search."
});
