Object.assign(window.SPEAKER_NOTES, {
  "a1": "If terminology becomes ambiguous in questions, I use three distinctions. A Wist dialect is a language profile or configuration. A Judgement is a contextual semantic claim. An Obligation is a correctness goal that must be discharged before a transformation or lowering decision is legal.",
  "a2": "Evidence is not just provenance. Depending on the property, evidence may come from a trusted policy, a certificate, a proof-producing analysis, or re-analysis on the current representation. Provenance can tell us where a fact came from, but provenance by itself does not necessarily prove that the fact is true or still valid.",
  "a3": "Any semantic knowledge system also needs explicit invalidation. Transformations change representations and assumptions, so stale knowledge is a correctness risk. The important question is not only what we know, but whether that knowledge is valid at this revision and in this context.",
  "a4": "This is why I do not assume universal inverse lowering. Transformations can split one operation into many, merge several operations into one, fuse them, or delete them entirely. Those cases make faithful reconstruction of the previous representation impossible in general.",
  "a5": "The implementation boundary is deliberate. Current UniversalToolchain includes configuration, Feature and Contribution planning, routes and frozen plans, a typed AIR rewrite, and Interpreter versus CIL parity tests. Judgements, obligations, and generalized semantic projection are proposed research directions rather than current implementation claims.",
  "a6": "Plugin systems are useful prior art for loading, lifecycle, and extension APIs. But loading a plugin does not by itself define semantic composition, route legality, or how independently authored components exchange semantic knowledge. Those are the additional problems this talk is separating out.",
  "a7": "Reflection is not essential to the architecture. Current generic authoring can use explicit typed registration, and the current capability projection uses bounded reflection only over implementation types that the plan has already selected. If trimming or Native AOT becomes a requirement, source-generated registration is a plausible replacement. I am not claiming broad Native AOT support or arbitrary external-package auto-discovery today."
});
