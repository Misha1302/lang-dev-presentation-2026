Object.assign(window.SPEAKER_NOTES,{"a1":"ЗАЧЕМ: быстро открыть exact source map для спорных claims. СКАЗАТЬ: planning/runtime evidence и compiler semantic/capability evidence pinned к одному UniversalToolchain snapshot. НЕ ПЕРЕОБЕЩАТЬ: имена классов не доказательство — при споре открываем implementation и tests.","a2":"ЗАЧЕМ: ограничить смысл PlanHash. СКАЗАТЬ: это canonical identity expressed resolved plan, полезная для drift/reproducibility/testing. НЕ ПЕРЕОБЕЩАТЬ: не semantic equivalence, security attestation, program identity или performance equality.","a3":"ЗАЧЕМ: ответить на DI/builder objection. СКАЗАТЬ: DI materializes already-known bindings; planning добавляет earlier whole-language resolution of dependencies, conflicts, provider ambiguity, ordering and routes. НЕ ПЕРЕОБЕЩАТЬ: DI остаётся отличным инструментом после resolution.","a4":"ЗАЧЕМ: позиционировать без novelty claim. СКАЗАТЬ: LLVM/MLIR/Racket/MPS решают родственные задачи на других слоях; этот talk про whole runnable-language planning boundary и explicit local rewrite evidence. НЕ ПЕРЕОБЕЩАТЬ: не говорить first/better/more extensible.","a5":"ЗАЧЕМ: отделить deterministic protocol от semantics. СКАЗАТЬ: route может воспроизводимо выбираться по expressed contracts/cost и всё равно быть семантически неподходящим. НЕ ПЕРЕОБЕЩАТЬ: route Cost — planning metric, не execution latency.","a6":"ЗАЧЕМ: провести security boundary. СКАЗАТЬ: restricted composition/interop policy ограничивает choices, но не создаёт process isolation или malicious-code containment. НЕ ПЕРЕОБЕЩАТЬ: valid plan ≠ sandbox.","a7":"ЗАЧЕМ: ответить на performance attack без main-deck перегруза. СКАЗАТЬ: planning может убрать open decision из repeated path, но interfaces/dispatch/allocations/validation остаются возможными. Четыре cost stages требуют measurement. НЕ ПЕРЕОБЕЩАТЬ: no re-planning ≠ zero overhead or faster.","a8":"ЗАЧЕМ: ответить «what if metadata lies?». СКАЗАТЬ: optimizer consumes explicit descriptor and additionally requires trusted level, но correctness descriptor is a trust/specification boundary. НЕ ПЕРЕОБЕЩАТЬ: metadata не становится truthful от того, что typed.","a9":"ЗАЧЕМ: показать IR contract mechanism без превращения в SSA lecture. СКАЗАТЬ: supplied pass order is candidate order; pipeline checks required facts/capabilities before pass and updates produced/preserved/invalidated facts after. НЕ ПЕРЕОБЕЩАТЬ: это не automatic scheduler. EGraph module — bounded straight-line symbolic simplifier, не general equality saturation.","a10":"ЗАЧЕМ: иметь Q&A weapon по maturity. СКАЗАТЬ: planner/runtime, semantic-gated CSE/folding, Wist capability gating and SSA contract checks implemented; e-graph and specialization scopes bounded; performance needs measurement; general semantic proof and automatic scheduling not current. НЕ ПЕРЕОБЕЩАТЬ: preliminary work explicitly labeled."});
for(const slide of document.querySelectorAll(".slide[data-note-key]")){slide.dataset.notes=window.SPEAKER_NOTES[slide.dataset.noteKey]||"";}
