Object.assign(window.SPEAKER_NOTES,{"m11":"ЗАЧЕМ: заменить прежний conceptual demo и любые simulated planner traces реальным current-source path. СКАЗАТЬ: demo состоит из двух коротких частей. Первая строит descriptor package с consumer, provider.a и provider.b. Compile возвращает UTL2002; затем PreferCapabilityProvider выбирает provider.a и corrected plan печатается. Вторая часть использует LanguagePackageBuilder из LanguageAuthoring. Transformer превращает SourceText «41» в int 41, backend executor возвращает value + 1, UseRouteRuntime добавляет route runtime. LanguageCompiler строит executable plan; программа печатает PlanHash, RuntimeProvider и каждый route step. После этого LanguageRuntime.Create(plan, componentSources) materializes exactly planned components, а Run возвращает 42. Важная operational detail: presentation CI checkout-ит truth repo на exact commit 36206b6 и project-references эти sources, поэтому demo validation не зависит от опубликованного NuGet package. ПЕРЕХОД: после доказательства implementation можно обсуждать, когда этот infrastructure cost оправдан. ДЕТАЛЬ: demo intentionally минимален: он не пытается показать parser framework, optimizations или богатый DSL. Его задача — максимально изолированно доказать ownership boundary и избежать театральной сложности live coding. НЕ ПЕРЕОБЕЩАТЬ: если CI source checkout недоступен, fallback обязан называться blocker, а не успешным demo.","m12":"ЗАЧЕМ: превратить case study в reusable engineering criterion. СКАЗАТЬ: вопрос не «нужен ли всем UniversalToolchain», а «где сейчас живут global composition decisions». Если один host владеет всеми компонентами и configuration стабильна, planner добавляет лишнюю abstraction. Если packages независимы и host должен выбирать providers, строить routes, применять ordering/conflicts и получать early diagnostics, отдельный planning stage централизует ответственность. Окупаемость измеряется не количеством plugins, а количеством решений, которые нельзя локально принять корректно. Это помогает отвечать на вопрос «почему не DI container»: DI хорошо materializes known bindings, но здесь до materialization нужно построить domain-specific validated plan с artifact contracts и deterministic identity. И наоборот, если ваш graph тривиален, не надо подменять simplicity красивым solver-ом. ПЕРЕХОД: planner решает проблему, но создаёт новую цену — composition protocol становится частью архитектуры. ДЕТАЛЬ: критерий помогает не строить planner заранее. Сначала ищем repeated whole-system decisions и concrete failure modes; только затем вводим explicit model, diagnostics и canonical result. НЕ ПЕРЕОБЕЩАТЬ: не утверждать, что planning layer автоматически уменьшает кодовую базу или onboarding cost.","m13":"ЗАЧЕМ: защитить доклад от ложной novelty framing. СКАЗАТЬ: language extensibility имеет сильный prior art; я не утверждаю «мы первыми придумали composition» и не сравниваю зрелость ecosystems. Эти системы решают разные, часто более широкие задачи. Здесь design point уже: explicit semantic planner для provider/contribution/artifact-route/runtime composition в .NET и строгая planner/runtime ownership boundary. Такой framing полезнее маркетинговой таблицы features, потому что аудитория может сравнить архитектурные choices. Explicit host pipeline остаётся первой альтернативой; Racket/#lang показывает другой language boot model; MPS — полноценный language workbench; MontiCore — component-oriented DSL tooling. UniversalToolchain в этом докладе — case study конкретного staging решения, а не победитель benchmark-а framework-ов. ПЕРЕХОД: после positioning важно честно назвать стоимость и закрытые части protocol. ДЕТАЛЬ: prior-art slide нужен не для сравнительной оценки products, а чтобы показать, что проблема language composition имеет много established решений. Здесь интересен именно выбранный boundary в конкретном .NET implementation. НЕ ПЕРЕОБЕЩАТЬ: не делать claims «better», «first», «more extensible» без сопоставимого исследования и критериев.","m14":"ЗАЧЕМ: сделать architecture trade-off симметричным. СКАЗАТЬ: independent packages становятся open-ended в рамках protocol, но сам protocol не бесконечно open-world. LanguageContributionDescriptor знает конкретные concepts: slots, requires/provides capabilities, conflicts, transformations, runtime inputs, ordering. Если завтра появится принципиально новый вид composition semantics, возможно, придётся менять planner core и canonicalization, а не просто добавить package. Это нормальная цена explicit model. Дополнительно появляются testing obligations: deterministic resolution, lock identity, exact runtime binding, lifecycle, failure diagnostics и route assembly. В source snapshot эти обязанности заметны отдельными regression tests и verifier-ом. Зато benefit тоже concrete: errors вроде UTL2002 возникают до runtime; runtime не угадывает alternative; plan можно inspect и hash. Архитектура хороша, когда эта predictability ценнее infrastructure cost. ПЕРЕХОД: ещё одна граница — даже идеальный structural protocol не доказывает semantic compatibility. ДЕТАЛЬ: protocol evolution требует versioning discipline. Current code уже проверяет ToolchainApi version на planning/runtime boundary; несовместимость должна становиться explicit diagnostic, а не случайным MethodNotFound позже. НЕ ПЕРЕОБЕЩАТЬ: не подавать partially closed-world nature как случайный implementation detail; это design boundary.","m15":"ЗАЧЕМ: поставить формальную границу evidence. СКАЗАТЬ: LanguageArtifactRoute.ContractsConnect проверяет artifact kind и typed value identity; contribution resolution проверяет declared requirements/conflicts; runtime проверяет exact registrations и policy traits. Всё это ценно, но это не theorem prover. Два компонента могут формально обмениваться одним `AIR` contract и по-разному интерпретировать hidden invariant, если invariant не выражен protocol-ом. Поэтому semantic safety требует дополнительных mechanisms: richer contracts, relational tests, property tests, integration suites или domain-specific verification. Аналогично наличие двух backend routes не доказывает одинаковое observable behavior; это тестируется отдельно. И тем более никакая structural validation сама по себе не говорит о performance. Этот слайд заранее отвечает на вопрос «можно ли доверять planner-у как полной проверке совместимости»: нет, он гарантирует то, что модель умеет выразить. ПЕРЕХОД: финальный takeaway теперь можно сформулировать без framework evangelism. ДЕТАЛЬ: hidden invariants лучше поднимать в typed artifact identities, capabilities или explicit policy там, где это возможно. Всё, что остаётся невыраженным, должно покрываться tests и documented assumptions. НЕ ПЕРЕОБЕЩАТЬ: не использовать слово «proof» без уточнения уровня contract.","m16":"ЗАЧЕМ: закончить переносимой идеей, а не призывом установить package. СКАЗАТЬ: главный вопрос, который я хочу оставить аудитории: где в вашей compiler/language architecture принимаются решения, требующие знания всей конфигурации? Если ответ «понемногу в plugins, DI registration order, runtime factories и host glue», это сигнал проверить ownership. UniversalToolchain case study показывает один вариант: local packages объявляют constraints; LanguageCompiler централизованно разрешает их в immutable LanguagePlan; LanguageRuntime materializes exactly that plan and executes it. Title «make the abstractions disappear» в этой формулировке означает: после staging provider/route/runtime alternatives больше не являются runtime decisions. Остальные abstraction costs честно остаются и могут оптимизироваться отдельно. Это делает архитектуру тестируемой: planner failures можно воспроизводить, plan inspect/hash, runtime exact binding проверять regression tests. ПЕРЕХОД: дальше Q&A; при вопросах открыть appendix с evidence map, real demo output и performance claim boundary. ДЕТАЛЬ: если аудитория запомнит одну вещь, пусть это будет separation of concerns: extensions describe local possibilities, planner resolves global choices, runtime executes the frozen result and validates exact binding. НЕ ПЕРЕОБЕЩАТЬ: в финале не добавлять новых claims, которых не было доказано ранее.","a1":"Appendix note: этот слайд открываю, если спрашивают «где именно это в коде». Все перечисленные files проверены на одном commit. Разделение planner/runtime следует из public entrypoints и tests, а не только из class names.","a2":"Appendix note: fallback text намеренно не подставляет fictional PlanHash или route cost. CI artifact `demo-source-output` содержит фактический stdout exact-source run. Если demo не прошёл, нужно показывать failure как blocker, а не этот слайд как доказательство успеха.","a3":"Appendix note: если вопрос звучит «а extensibility не ударит по performance?», ответ разделён на две части. Planner cost вынесен до execution boundary по architecture; runtime всё равно имеет validation/materialization/dispatch costs. Для их размера нужен benchmark. Никаких цифр доклад не придумывает.","a4":"Appendix note: planner tests должны фиксировать ambiguity, conflicts, order, route и determinism. Runtime tests — exact registration binding, policy validation, lifecycle и result. Relational backend tests дополняют, но не заменяют semantic specification."});
for(const slide of document.querySelectorAll(".slide[data-note-key]")){slide.dataset.notes=window.SPEAKER_NOTES[slide.dataset.noteKey]||"";}
