Object.assign(window.SPEAKER_NOTES,{"a1":"ЗАЧЕМ: быстро показать, где лежит source evidence для центральных claims. СКАЗАТЬ: planning и runtime разведены не только словами презентации — перечислены конкретные compiler, plan, route, runtime и exact-binding files на одном pinned UniversalToolchain snapshot. НЕ ПЕРЕОБЕЩАТЬ: список классов не является доказательством сам по себе; при споре открываем implementation и tests.","a2":"ЗАЧЕМ: точно ограничить смысл PlanHash. СКАЗАТЬ: это canonical identity разрешённой configuration, полезная для drift detection, reproducibility и regression tests. Она не доказывает semantic equivalence, security attestation, program identity или performance equality. НЕ ПЕРЕОБЕЩАТЬ: не называть PlanHash доказательством корректности или безопасности.","a3":"ЗАЧЕМ: ответить на вопрос, почему нельзя просто остановиться на DI или builder. СКАЗАТЬ: DI отлично materializes уже известные bindings; planning добавляет domain-specific whole-language resolution — dependencies, conflicts, provider ambiguity, ordering и artifact routes — до materialization. НЕ ПЕРЕОБЕЩАТЬ: DI не объявляется плохим; после planning runtime всё ещё может строить обычные objects привычными средствами.","a4":"ЗАЧЕМ: поставить UniversalToolchain в контекст prior art без novelty marketing. СКАЗАТЬ: Racket/#lang, MLIR dialects, JetBrains MPS и MontiCore решают родственные composition/extensibility задачи на разных слоях и с разными boundaries. Этот слайд — positioning, не ranking. НЕ ПЕРЕОБЕЩАТЬ: не утверждать first, better или more extensible без отдельного сравнительного исследования.","a5":"ЗАЧЕМ: отделить deterministic protocol от semantic correctness. СКАЗАТЬ: planner может детерминированно выбрать route по выраженным contracts, preferences и planning Cost, но две реализации всё равно могут иметь разные semantics. Equal-cost/tie behavior должен быть частью тестируемого protocol. НЕ ПЕРЕОБЕЩАТЬ: route Cost — planning metric, не measured execution latency.","a6":"ЗАЧЕМ: провести security boundary. СКАЗАТЬ: restricted composition может ограничивать host interop, selected features, runtime policy и composition choices, но сама по себе не создаёт process isolation или sandbox boundary. НЕ ПЕРЕОБЕЩАТЬ: слово restricted не использовать как синоним sandbox; semantic safety и security требуют отдельных механизмов.","a7":"ЗАЧЕМ: ответить, является ли design Wist-specific. СКАЗАТЬ: TensorRules — только transfer experiment: он показывает, что generic planning/runtime APIs синтаксически не привязаны к Wist. Это полезное вторичное evidence, но не второй production language story. НЕ ПЕРЕОБЕЩАТЬ: один experiment не доказывает универсальность architecture для любых DSL или compiler ecosystems.","a8":"ЗАЧЕМ: закончить appendix жёсткой границей измеренного evidence. СКАЗАТЬ: current source подтверждает single planner, resolved LanguagePlan, exact runtime binding, Wist presets/frontend и отсутствие global re-planning в Run. Planning latency, runtime creation, first-execution и steady-state overhead требуют benchmark artifact. НЕ ПЕРЕОБЕЩАТЬ: без exact-current raw measurements не называем ни одной performance цифры."});
for(const slide of document.querySelectorAll(".slide[data-note-key]")){slide.dataset.notes=window.SPEAKER_NOTES[slide.dataset.noteKey]||"";}
