Object.assign(window.SPEAKER_NOTES,{"a1":"ЗАЧЕМ: быстро открыть exact source map для спорных load-bearing claims. СКАЗАТЬ: main talk опирается на LanguageCompiler, LanguagePlan, route phase, LanguageRuntime, authoring tests и WistEngine staging; compiler optimization details сознательно вынесены из main. НЕ ПЕРЕОБЕЩАТЬ: имена классов не доказательство — при споре открываем implementation и tests.","a2":"ЗАЧЕМ: ограничить смысл PlanHash. СКАЗАТЬ: это canonical identity expressed resolved plan, полезная для drift/reproducibility/testing. НЕ ПЕРЕОБЕЩАТЬ: не semantic equivalence, security attestation, program identity или performance equality.","a3":"ЗАЧЕМ: ответить на DI/builder objection. СКАЗАТЬ: DI materializes already-known bindings; planning добавляет earlier whole-language resolution of dependencies, conflicts, provider ambiguity, ordering and routes. НЕ ПЕРЕОБЕЩАТЬ: DI остаётся отличным инструментом до или после resolution, если он владеет реальной задачей.","a4":"ЗАЧЕМ: позиционировать без novelty claim. СКАЗАТЬ: LLVM pass management, MLIR conversion, Racket и MPS решают родственные задачи на других слоях; этот talk про whole runnable-language composition → immutable plan → exact materialization. НЕ ПЕРЕОБЕЩАТЬ: не говорить first, better или more extensible.","a5":"ЗАЧЕМ: отделить deterministic protocol от semantics. СКАЗАТЬ: route может воспроизводимо выбираться по expressed contracts/cost и всё равно быть семантически неподходящим. НЕ ПЕРЕОБЕЩАТЬ: route Cost — planning metric, не execution latency.","a6":"ЗАЧЕМ: провести security boundary. СКАЗАТЬ: restricted composition/interop policy ограничивает choices, но не создаёт process isolation или malicious-code containment. НЕ ПЕРЕОБЕЩАТЬ: valid plan ≠ sandbox.","a7":"ЗАЧЕМ: ответить на performance attack. СКАЗАТЬ: planning может убрать open global decision из repeated path, но interfaces/dispatch/allocations/validation остаются возможными. Planning, materialization, first run и steady state требуют отдельных measurements. НЕ ПЕРЕОБЕЩАТЬ: no re-planning ≠ zero overhead or faster.","a8":"ЗАЧЕМ: сохранить вынесенную из main compiler-semantic тему для Q&A. СКАЗАТЬ: optimizer consumes explicit descriptor and additionally requires trusted level, но correctness descriptor is a trust/specification boundary. НЕ ПЕРЕОБЕЩАТЬ: metadata не становится truthful от того, что typed.","a9":"ЗАЧЕМ: показать IR contract mechanism без превращения main talk в SSA lecture. СКАЗАТЬ: supplied pass order is candidate order; pipeline checks required facts/capabilities before pass and updates produced/preserved/invalidated facts after. НЕ ПЕРЕОБЕЩАТЬ: это не automatic scheduler. EGraph module — bounded straight-line symbolic simplifier, не general equality saturation.","a10":"ЗАЧЕМ: иметь Q&A weapon по maturity. СКАЗАТЬ: planner/runtime, deterministic structural routes and exact binding implemented; optimization helpers also exist but не являются core narrative. Performance needs measurement; general semantic route proof, sandboxing and automatic scheduling not current. НЕ ПЕРЕОБЕЩАТЬ: preliminary work explicitly labeled."});
for(const slide of document.querySelectorAll(".slide[data-note-key]")){slide.dataset.notes=window.SPEAKER_NOTES[slide.dataset.noteKey]||"";}
