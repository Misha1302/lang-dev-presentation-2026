window.SPEAKER_SCRIPT = Object.freeze({
  ...window.SPEAKER_SCRIPT,
  "pa1": "The useful comparison is not a scorecard. It is: what is the first-class extensible object in each system?\n\nLLVM is mostly a downstream anchor. MLIR is the strongest neighbour on the representation axis: dialects, interfaces, transformation control and staged conversion. Racket is strongest on language-oriented programming.\n\nBut the closest source-language precedents are Neverlang, ableC with Silver, and MontiCore. Neverlang is close to whole-language composition: features become modules, roles and slices, then language descriptors compose them into compiler or interpreter variants. ableC is especially important for independently developed extensions because it has modular analyses for safe composition. MontiCore is close because it composes textual language components and has product-line work around provided and required interfaces.\n\nSo the UT claim must be narrower and stronger. The point is not that language composition exists. It clearly exists. The stronger target is representation-neutral compiler composition: a profile resolves capabilities, providers, routes and backends, while semantic evidence is shared without requiring every package to use the same universal IR.",
  "pa2": "This is the distinction I need to make much more explicit. MLIR and UT both use the word composition, but they make different things first-class.\n\nIn MLIR, the object being transformed is the program representation. Multiple dialects can coexist in the same module. Interfaces let transformations remain generic. Pass pipelines and the Transform dialect orchestrate transformations, and dialect conversion decides whether operations are legal for a target representation.\n\nSo UT cannot honestly say that MLIR lacks pipelines, orchestration, interfaces or legality. It has all of those.\n\nThe narrower UT question happens one level earlier: given a requested language profile and target policy, which compiler components, providers, representation routes and backend will form the concrete compiler? The result is a LanguagePlan, which is a compiler-configuration artifact, not a program IR. That plan is then reused while compiling programs.\n\nThis is why MLIR can be inside a UT plan rather than being replaced by UT. The strongest formulation is: MLIR makes program representations and transformations extensible; UT is exploring first-class resolution of the compiler configuration that will use those mechanisms.",
  "pa3": "Two systems deserve special attention because they attack the source-language side more directly than MLIR.\n\nNeverlang decomposes language features into modules, roles and slices. Those pieces can be independently compiled, tested and distributed, and language descriptors compose them into compiler or interpreter variants. Later work explicitly treats these variants as language product lines. That is very close to the motivation of UniversalToolchain, and I should say so.\n\nableC and Silver are a different but equally serious challenge. They focus on independently developed extensions to a host C language and provide modular analyses so extensions that pass the checks can be composed into a deterministic parser and a well-defined attribute grammar. In other words, they have stronger prior art for safe independent extension composition than UT currently does.\n\nThe remaining research question is therefore not whether modular language composition already exists. It clearly does. The question is whether UT's broader provider-and-route planning across compiler stages, plus cross-representation semantic evidence, buys something measurable beyond these existing models.",
  "pa4": "This is the correction to the way I should talk about the current implementation.\n\nCurrent Wist has a real AIR with Push, Drop, jumps, labels and intrinsics. It is stack-oriented, and the verifier checks stack discipline and backend capabilities. That is useful evidence that the project can build and execute a compiler pipeline, but it should not be presented as the universal architecture.\n\nThe generic SDK already points in a better direction: language definitions produce selected contributions, typed artifact routes, passes, backends and a LanguagePlan. The ideal target is to demote AIR from framework core to one representation package.\n\nSo I should say: Push and Drop are not UT primitives. They belong to a StackMachinePack. SSA belongs to an SSAPack. MLIR can belong to an MLIRProvider. UT core should not know any of these execution concepts.",
  "pa5": "Here is the stronger architecture slogan: no universal IR; a universal way to compose IRs, semantics and compilers.\n\nThe core should not execute a program. The core should know identities, artifact contracts, providers, capabilities, constraints, obligations, semantic evidence, correspondence, validity and the frozen plan.\n\nEverything code-like moves out. Syntax, types, stack machines, SSA, MLIR, domain graphs and backends become packages. A plan chooses which of them are present for a particular language profile.\n\nThis is why the thesis is not just minimalism. The core is minimal in a very specific way: it is a meta-kernel for compiler composition, not a tiny virtual machine.",
  "pa6": "This is the cleanest boundary against the closest prior art.\n\nNeverlang is close, but its composition model is still organized around syntax, parse trees, roles and slices. ableC with Silver is close, but it is organized around an extensible C host language, attribute grammars and forwarding. MontiCore is close, but its center of gravity is grammar, AST, context conditions and generators.\n\nIdeal UT tries to move the substrate one level lower. A package may define syntax, but it does not have to. A package may define an IR, but not the one true IR. A package may define an executor, an analysis, a semantic theory or a representation route.\n\nThis does not make UT automatically better. It creates a sharper research claim: can this representation-neutral substrate still provide enough static discipline and useful composition?",
  "pa7": "This slide makes the consequence concrete.\n\nThe stack machine becomes a package that owns Push, Drop, stack typing, stack verification and stack lowering. SSA becomes a package that owns values, blocks, dominance and SSA verification. MLIR becomes a provider that owns dialect import or export, conversion legality and transform integration. A domain graph package can be completely non-code-like.\n\nThe same is true for intrinsics. Instead of treating Intrinsic as a universal opcode, we can treat something like math.multiply as a semantic concept. Different representations may realize it as a stack operation, an SSA operation, an MLIR operation, a CIL instruction, a runtime call or a GPU operation.\n\nThat is the architectural payoff of removing the execution core.",
  "pa8": "This final slide keeps the claim honest. The ideal architecture is only meaningful if it passes experiments.\n\nFirst, remove AIR from the core path: build one route through StackIR and another through SSA or MLIR without teaching the core Push, Drop or Jump.\n\nSecond, test cross-representation SafeIndex: range facts from one representation, shape facts from another, specialization in a third, with zero direct producer-to-consumer adapters.\n\nThird, replace the engine while preserving obligations and diagnostics. If representation-specific invariants leak into the planner, the thesis fails.\n\nThis is the final message: UT should not claim it invented modular languages. It should claim, and then prove, representation-neutral compiler composition without a universal IR.",
  "paa1": "These are the systems I would keep ready for Q and A rather than putting all of them into the main comparison.\n\nMPS is a comprehensive projectional language workbench, so it is the right answer to a question about full language authoring and integrated IDE support. Spoofax and Rascal are strong textual language-engineering environments covering syntax, semantics, transformations and tooling. Truffle is important for a different reason: it attacks language implementation performance with self-optimizing AST interpreters and partial evaluation.\n\nXtext and parser generators are useful baselines, but they are not the strongest architectural alternatives."
});
