window.SPEAKER_SCRIPT = Object.freeze({
  ...window.SPEAKER_SCRIPT,
  "pa1": "This slide is the simplified comparison I want the audience to remember. Each system makes a different thing first-class. MLIR makes extensible IR structure and semantics first-class. MPS makes global generation planning and checkpoints first-class. Neverlang makes language features and language products first-class. GEMOC, Melange and BCOoL make executable models and coordinated execution first-class. GraalVM and Truffle make self-optimizing language runtimes and polyglot values first-class. Rascal makes language tooling itself easy to implement in one meta-language.\n\nIdeal UT is not claiming that any one of those ideas is new. The target is their intersection: independently authored compiler representations, analyses, transformations and engines are resolved into one compiler plan, and semantic evidence can cross representation boundaries without forcing everyone into one program representation or one implementation stack.",
  "pa2": "MLIR is the most important technical comparison, so I need to be precise. MLIR is not a single opcode set. It is a shared structural IR world: operations, regions, blocks, values, types and attributes. Dialects extend that world. Interfaces, external models, dataflow, effects, the Transform dialect and conversion legality already solve many things people might incorrectly attribute to UT.\n\nThe UT boundary is therefore narrower. Ideal UT asks whether MLIR is selected at all. A profile may choose an MLIR route, a stack route, an SSA library, an AST interpreter, a domain graph or a mixture of them. The hard claim is not that MLIR is weak. The hard claim is that semantic evidence and obligations can move between components that do not all live inside the MLIR object model.",
  "pa3": "This slide removes three common confusions. GEMOC is close because it coordinates heterogeneous executable languages and execution engines, but its primary question is runtime behavioral coordination between executable models. UT wants compile-time planning and optimizer evidence across compiler subsystems.\n\nGraalVM and Truffle are close because they show how a common protocol can avoid pairwise adapters between languages. But the protocol is mainly about runtime values and operations, while UT wants a similar reduction for compiler facts and transformations.\n\nRascal is close because it is a very productive meta-language for building language tooling. The challenge for UT is different: can independently authored tooling written in different stacks still be composed without forcing everyone to rewrite inside one meta-language?",
  "pa4": "This slide is only an implementation witness, not the object of the prior-art comparison. Current Wist has a real AIR with Push, Drop, jumps, labels and intrinsics. It is stack-oriented, and the verifier checks stack discipline and backend capabilities. That shows the project can build and execute a compiler pipeline, but it should not define the research architecture.\n\nThe generic SDK already points in a better direction: language definitions produce selected contributions, typed artifact routes, passes, backends and a LanguagePlan. The ideal target is to demote AIR from framework core to one representation package. Push and Drop belong to a StackMachinePack. SSA belongs to an SSAPack. MLIR can belong to an MLIRProvider. The comparison is against that target, not against today's implementation depth.",
  "pa5": "The slogan is: no universal program IR; one meta-layer for routes, engines and evidence. The important part is not only removing AIR from the core. MLIR, MPS, GEMOC, Graal and Rascal already cover many individual pieces of the story. The stronger target is to keep a small meta-kernel that knows identities, artifact contracts, providers, capabilities, constraints, transformation obligations, semantic evidence, correspondence, invalidation and the resolved plan, but does not define the program's structure itself.\n\nSo the core should not know Operation and Value, AST node shape, SSA blocks, stack opcodes or VM instructions. It should know how to decide which package owns those concepts for a given language profile.",
  "pa6": "This is the plain-English boundary slide. MPS answers how language generators can be globally ordered. GEMOC answers how executable languages can coordinate behavior. Graal and Truffle answer how language runtimes can specialize and interoperate. Rascal answers how language tooling can be implemented productively in one meta-language.\n\nIdeal UT asks a different question: how do independent compiler subsystems become one compiler? The hypothesis is provider, route and evidence resolution. It only wins if it gives measurably fewer pairwise adapters while keeping transformations fail-closed when evidence is stale, contradictory or unsupported.",
  "pa7": "This slide makes the target concrete. The stack machine becomes a package that owns Push, Drop, stack typing, stack verification and stack lowering. SSA becomes a package that owns values, blocks, dominance and SSA verification. MLIR becomes a provider that owns its dialects, conversion legality and transform integration. A domain graph package can be completely non-code-like.\n\nThe same is true for operations such as multiplication. The meta-layer should reason about a semantic concept and its obligations, while each representation realizes it differently: stack operation, SSA call, MLIR operation, CIL instruction, runtime call or GPU operation. The payoff is preserving composition and optimization while refusing to impose one universal opcode vocabulary.",
  "pa8": "The final slide keeps the target falsifiable. First, non-MLIR and MLIR routes must be addable as providers without teaching the core their instruction semantics. Second, the SafeIndex experiment must pass across representations: range facts from one representation, shape facts from another and specialization in a third, with no direct producer-to-consumer adapters and no unsafe discharge under stale or contradictory evidence.\n\nThird, UT must beat the strongest ordinary baseline: MLIR interfaces and dataflow where applicable, MPS-like checkpoints, Graal-style protocols, Rascal-style shared tooling and explicit adapters. If that gives the same safety and coupling with less infrastructure, the UT semantic meta-layer should be removed. That makes the claim research-grade instead of marketing-grade.",
  "paa1": "These appendix systems remain useful for questions from the audience. LLVM New PM is the baseline for cached analyses, pass pipelines and explicit preservation and invalidation. MontiCore is a strong language-component workbench with grammar composition, context conditions and generators. Racket is the classic language-oriented-programming reference with readers, expanders, macros and #lang. K Framework is the formal-semantics route: define executable semantics and derive tooling from it.\n\nThe main deck now focuses on the projects that make the Ideal UT boundary easiest to explain."
});
