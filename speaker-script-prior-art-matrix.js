window.SPEAKER_SCRIPT = Object.freeze({
  ...window.SPEAKER_SCRIPT,
  "pa1": "The useful comparison is not a scorecard. It is: what is the first-class extensible object in each system?\n\nLLVM is mostly a downstream anchor. MLIR is the strongest neighbour on the representation axis: dialects, interfaces, transformation control and staged conversion. Racket is strongest on language-oriented programming.\n\nBut the research changed my view of the closest source-language precedents. Neverlang is very close to the whole-language composition problem: features are implemented as modules, roles and slices, then composed into compiler or interpreter variants. ableC with Silver is a particularly important precedent for independently developed language extensions because it has modular analyses for whether certified extensions compose safely. MontiCore is also closer than a generic workbench comparison: it explicitly composes textual language components and recent work models language product lines with provided and required component interfaces.\n\nSo I should not claim one universal competitor. UT has to be compared against the strongest precedent on each dimension. The current UT thesis is that a language profile is resolved into one inspectable compiler plan across capabilities, providers, routes and backends, and then we ask whether a shared semantic-evidence layer can further reduce cross-component coupling. The last part is research, not an established advantage.",
  "pa2": "This is the distinction I need to make much more explicit. MLIR and UT both use the word composition, but they make different things first-class.\n\nIn MLIR, the object being transformed is the program representation. Multiple dialects can coexist in the same module. Interfaces let transformations remain generic. Pass pipelines and the Transform dialect orchestrate transformations, and dialect conversion decides whether operations are legal for a target representation.\n\nSo UT cannot honestly say that MLIR lacks pipelines, orchestration, interfaces or legality. It has all of those.\n\nThe narrower UT question happens one level earlier: given a requested language profile and target policy, which compiler components, providers, representation routes and backend will form the concrete compiler? The result is a LanguagePlan, which is a compiler-configuration artifact, not a program IR. That plan is then reused while compiling programs.\n\nThis is why MLIR can be inside a UT plan rather than being replaced by UT. The strongest formulation is: MLIR makes program representations and transformations extensible; UT is exploring first-class resolution of the compiler configuration that will use those mechanisms.",
  "pa3": "Two systems deserve special attention because they attack the source-language side more directly than MLIR.\n\nNeverlang decomposes language features into modules, roles and slices. Those pieces can be independently compiled, tested and distributed, and language descriptors compose them into compiler or interpreter variants. Later work explicitly treats these variants as language product lines. That is very close to the motivation of UniversalToolchain, and I should say so.\n\nableC and Silver are a different but equally serious challenge. They focus on independently developed extensions to a host C language and provide modular analyses so extensions that pass the checks can be composed into a deterministic parser and a well-defined attribute grammar. In other words, they have stronger prior art for safe independent extension composition than UT currently does.\n\nThe remaining research question is therefore not whether modular language composition already exists. It clearly does. The question is whether UT's broader provider-and-route planning across compiler stages, plus cross-representation semantic evidence, buys something measurable beyond these existing models.",
  "paa1": "These are the systems I would keep ready for Q and A rather than putting all of them into the main comparison.\n\nMPS is a comprehensive projectional language workbench, so it is the right answer to a question about full language authoring and integrated IDE support. Spoofax and Rascal are strong textual language-engineering environments covering syntax, semantics, transformations and tooling. Truffle is important for a different reason: it attacks language implementation performance with self-optimizing AST interpreters and partial evaluation.\n\nXtext and parser generators are useful baselines, but they are not the strongest architectural alternatives."
});
