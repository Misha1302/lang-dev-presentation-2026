window.SPEAKER_SCRIPT = Object.freeze({
  ...window.SPEAKER_SCRIPT,
  "pa1": "This slide is the simplified comparison I want the audience to remember. Each system makes a different thing first-class. MLIR makes extensible IR structure and semantics first-class. MPS makes global generation planning and checkpoints first-class. Neverlang makes language features and language products first-class. GEMOC, Melange and BCOoL make executable models and coordinated execution first-class. GraalVM and Truffle make self-optimizing language runtimes and polyglot values first-class. Rascal makes language tooling itself easy to implement in one meta-language.\n\nIdeal UT is not claiming that any one of those ideas is new. The target is their intersection: independently authored compiler representations, analyses, transformations and engines are resolved into one compiler plan, and semantic evidence can cross representation boundaries without forcing everyone into one program representation or one implementation stack.",
  "pa2": "MLIR is the strongest comparison. It already gives dialects, interfaces, external models, dataflow, effects, Transform dialect and conversion legality. UT must therefore claim only the space outside one mandatory MLIR program world: selecting heterogeneous representations and moving valid evidence between them.",
  "pa3": "This slide removes three common confusions. GEMOC is close because it coordinates heterogeneous executable languages and execution engines, but its primary question is runtime behavioral coordination between executable models. UT wants compile-time planning and optimizer evidence across compiler subsystems.\n\nGraalVM and Truffle are close because they show how a common protocol can avoid pairwise adapters between languages. But the protocol is mainly about runtime values and operations, while UT wants a similar reduction for compiler facts and transformations.\n\nRascal is close because it is a very productive meta-language for building language tooling. The challenge for UT is different: can independently authored tooling written in different stacks still be composed without forcing everyone to rewrite inside one meta-language?",
  "pa4": "This slide separates the current witness from the research target. Wist AIR is real and useful, but it is not the architecture. The target is a planner/evidence layer that can choose between representation packages without learning their instruction semantics.",
  "pa5": "The proposed core is deliberately small: identity, artifact contracts, providers, constraints, routes, obligations and evidence validity. Program structure belongs to packages such as StackIR, SSA, MLIR or a domain graph. If the core starts knowing Push, Value or Block, the design has failed.",
  "pa6": "This is the plain-English boundary slide. MPS answers how language generators can be globally ordered. GEMOC answers how executable languages can coordinate behavior. Graal and Truffle answer how language runtimes can specialize and interoperate. Rascal answers how language tooling can be implemented productively in one meta-language.\n\nIdeal UT asks a different question: how do independent compiler subsystems become one compiler? The hypothesis is provider, route and evidence resolution. It only wins if it gives measurably fewer pairwise adapters while keeping transformations fail-closed when evidence is stale, contradictory or unsupported.",
  "pa7": "Stack, SSA and MLIR should be selectable packages. Each package owns its own semantics and verifier. UT only coordinates how packages connect in a concrete plan. This keeps Wist AIR as a witness, not as a universal intermediate language.",
  "pa8": "This is the falsification slide. A non-MLIR route must not force instruction knowledge into the core. SafeIndex must work across representations without unsafe evidence reuse. And the whole thing must beat explicit adapters. Otherwise the meta-layer should be removed.",
  "paa1": "These appendix systems remain useful for questions from the audience. LLVM New PM is the baseline for cached analyses, pass pipelines and explicit preservation and invalidation. MontiCore is a strong language-component workbench with grammar composition, context conditions and generators. Racket is the classic language-oriented-programming reference with readers, expanders, macros and #lang. K Framework is the formal-semantics route: define executable semantics and derive tooling from it.\n\nThe main deck now focuses on the projects that make the Ideal UT boundary easiest to explain."
});
