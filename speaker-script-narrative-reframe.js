(() => {
  const previous = window.SPEAKER_SCRIPT || {};
  window.SPEAKER_SCRIPT = Object.freeze({
    ...previous,
    m1: `The talk has one high-level goal: keep language authoring open without forcing the final compiler to stay abstract. I will separate what is implemented, what we measured, and what is still a research hypothesis. The story has two concrete threads: composing language extensions, and then composing semantic knowledge for optimization.`,
    m2: `The starting point is deliberately conservative. A monolithic compiler is often the best design. Extensibility only earns its cost when we have a family of related languages, independent ownership, or repeated variation across the same compiler stages. The question is not how to make everything pluggable. The question is when reusable infrastructure actually pays for itself.`,
    nProblem: `There are two coupled scaling problems. A language feature is often a vertical slice across syntax, validation, analysis, lowering and tooling. Separately, an optimizer may need facts produced by analyses it does not own. The first creates implementation duplication; the second creates semantic coupling. The architecture has to address both without pretending they are the same problem.`,
    nSolutions: `This is not a green field. MLIR is a strong answer for extensible IR and semantic interfaces. MPS and Neverlang are strong answers for modular language construction and planning. LLVM has mature analysis preservation and invalidation. Graal demonstrates the power of a common protocol plus specialization. The research question is the gap between those boundaries, not whether these mechanisms exist.`,
    nMatrix: `The matrix is intentionally about responsibility boundaries rather than checkmarks. MLIR assumes a common MLIR program representation. MPS and Neverlang make their language framework the composition substrate. LLVM analysis machinery is excellent inside LLVM IR. Graal's protocol is runtime interop. Ideal UT is only interesting if a whole compiler can select heterogeneous representations and still exchange useful compiler evidence safely.`,
    nExternalBench: `Before talking about zero-cost abstractions, it is useful to look at external data. Amini and Niu measured MLIR APIs at EuroLLVM 2024 and showed that extensibility and richer traversal mechanisms have measurable compile-time costs. These numbers are microbenchmarks, not a comparison with UT. I use them for one point only: any abstraction layer has to measure its own overhead instead of assuming it disappears.`,
    nOverview: `At the highest level the proposal separates three questions. Authors publish reusable pieces against contracts. Resolution turns one requested language into one concrete compiler plan. Then the research part asks whether independent modules can exchange validity-scoped semantic evidence so later transformations can specialize safely. Planning exists today in UT; the semantic-evidence layer is the hypothesis.`,
    m7: `This status slide is a promise about the rest of the talk. Current UT gives implementation witnesses for routes, plans, one planning regression and bounded local deabstraction. We also have a small measured language-evolution study with mixed results. The central semantic-evidence story is still open research. I do not want those three states to blur together.`,
    r2: `The engineering trade-off is simpler than the previous terminology made it sound. Clone-and-own does less setup work. A reusable platform does more setup work in exchange for the possibility of cheaper later extensions. The relevant question is not whether the API feels complicated; it is whether the extra initial implementation work is repaid by future language changes.`,
    r3: `We froze the changes before implementing them so that the comparison would not move after seeing results. The same pricing-language changes were implemented in a fixed control, clone-and-own variants, and UT composition, with the same behavior oracle. Mixed and negative results were explicitly allowed.`,
    r4: `For the second extension, the additional implementation slice was smaller with UT: nineteen source lines versus thirty-five in the clone baseline. But total implementation at that point was still larger: one hundred and five versus sixty-one. So the simple conclusion is: this extension was cheaper to add, but the platform had not paid back its initial work.`,
    r5: `The next result is important because it prevents an easy sales story. We expected a shared rule change to propagate more cheaply through composition. A fair clone baseline centralized the same utility, and both approaches changed one logical site, one file and seven lines. In this workload that expected maintenance advantage did not appear.`,
    r6: `The ordinary shared-pipeline control is another guardrail. Both the ordinary pipeline and UT reused the downstream representation without touching variant-specific semantics. The ordinary pipeline needed even fewer isolated lines. That means downstream reuse is valuable, but it is not by itself evidence that source-language composition needs UT.`,
    m3: `Now we return to the concrete language example. A discount feature is not just grammar syntax. It affects validation, the semantic model, lowering, execution and diagnostics. That is why a single local plugin hook is often the wrong unit of reuse. We first understand the slice, and only then give it a name.`,
    m5: `I will call that reusable vertical slice a capability. The name is less important than the responsibility: the piece may own contributions across several compiler stages, while remaining smaller than a complete language. This is a general design concept; current Wist packages are only one implementation witness.`,
    m11: `A set of capabilities is still not a language. A concrete language profile selects the pieces, restrictions, policy and targets for one product. This separates the request from the resolution mechanism. The profile says what language is wanted; it should not need to encode every low-level route that can realize it.`,
    m12: `Once the request is separated from resolution, global interactions become visible. Providers may conflict, routes may have ordering constraints, and several backends may be possible. This is where a whole-configuration resolver becomes useful: it owns the answer to how this requested profile becomes one compiler.`,
    m40: `The earlier slide showed why a correct fact can become stale. Here is the concrete object we actually need: what proposition was established, which subject it describes, where and when it is valid, and which assumptions or producer evidence justify it. The formal model can name this a Judgement, but the mechanism should be understandable before the noun appears.`,
    m50: `This is the study that still has to be run before making a strong semantic-interoperability claim. The baseline is deliberately ordinary: local MLIR or LLVM-style interfaces, domain analyses and explicit adapters. The intervention is the shared evidence lifecycle. Correctness controls must include stale and contradictory facts and representation changes, and we must measure overhead as well as adapter count and precision.`,
    nRQ: `The conclusion is easier to trust if every research question has an explicit answer state. The engineering-work question has a partial answer from a small experiment. The planning question has a bounded implementation answer for structural feasibility. The semantic-evidence question is still open. The talk should leave those states visible rather than collapsing them into one claim that UT works.`,
    m52: `The final memory is three rules. Author locally only when variation justifies reusable infrastructure. Resolve globally when local choices interact. And before specializing away a safety check or another abstraction, require current valid evidence for the transformation's obligation. The strongest final condition is adversarial: if the shared layer does not beat explicit adapters on cost, precision and safety, remove it.`
  });
})();
