window.SPEAKER_NOTES={"m1":"ЗАЧЕМ: открыть доклад не API-туром, а архитектурным вопросом ownership. Название обещает исчезновение абстракций, поэтому сразу ограничиваю смысл: я не утверждаю, что интерфейсы магически исчезают или что extensibility бесплатна. Я говорю о staging композиционных решений. СКАЗАТЬ: в UniversalToolchain независимые пакеты описывают локальные возможности, а один semantic planner получает целую конфигурацию языка и превращает множество допустимых вариантов в конкретный LanguagePlan. После этого runtime не должен заново решать, какой provider, какой route и какой backend имелись в виду. Это и есть центральная deabstraction boundary доклада. Важно подчеркнуть, что тезис проверяем по текущему source snapshot, а не по старой презентационной модели. LanguageCompiler документирован как single public semantic planner, а LanguageRuntime материализует уже immutable plan. ПЕРЕХОД: прежде чем вводить planner, покажу сильнейшую альтернативу — обычный явный pipeline. ДЕТАЛЬ: если аудитория спросит, что здесь именно abstraction, отвечаю: множество допустимых composition choices и responsibility за их разрешение. Object model остаётся; меняется стадия, на которой неопределённость разрешается. НЕ ПЕРЕОБЕЩАТЬ: никаких zero-cost, «всё исчезает» или benchmark claims.","m2":"ЗАЧЕМ: убрать strawman. Planner не нужен просто потому, что слово «extensible» звучит современно. Если host знает parser, lowering, optimizations и backend заранее, ручная композиция прозрачнее: меньше infrastructure, проще debugging, очевиднее ownership. СКАЗАТЬ: я бы сам начинал именно так. Это baseline, с которым новая архитектура должна конкурировать по необходимости, а не по эстетике. Planner окупается только когда компоненты становятся независимыми, optional и поставляются разными пакетами, а решения о совместимости уже нельзя корректно принять внутри одного компонента. Здесь полезно мысленно отделить «можно подменить implementation» от «нужно согласовать целый граф». В первом случае DI или builder достаточно. Во втором появляются неоднозначность providers, route search, ordering, conflicts и выбор runtime. ПЕРЕХОД: следующий слайд показывает момент, когда локальная extensibility перестаёт быть локальной задачей. ДЕТАЛЬ: простой pipeline особенно полезен как контрольная группа для design review: каждую новую planner feature стоит оправдывать failure case, который нельзя чисто выразить на baseline без разрастания host glue. НЕ ПЕРЕОБЕЩАТЬ: не называть explicit pipeline примитивным или устаревшим — для закрытой конфигурации он часто лучше.","m3":"ЗАЧЕМ: сформулировать проблему, которую реально решает planner. Локальный пакет может честно объявить: «я предоставляю capability», «мне нужен такой artifact contract», «я конфликтую с этим contribution». Но он не видит всех остальных пакетов и не должен делать глобальный выбор за host. СКАЗАТЬ: глобальными становятся по крайней мере шесть классов решений — provider selection, ordering, conflicts, artifact routing, backend selection и runtime capabilities. Если распределить эти решения по extension code, получается скрытая зависимость от load order или host-specific glue. Если оставить их до execution, ошибка проявляется слишком поздно. Поэтому нужен отдельный owner, который видит целую configuration graph. В текущем API это не абстрактный «solver somewhere»: LanguageCompiler получает LanguagePackageRegistry и LanguageDefinition. ПЕРЕХОД: теперь можно разделить обязанности modules и planner на конкретных сущностях. ДЕТАЛЬ: слово global здесь означает не process-wide singleton, а необходимость видеть whole selected language definition. Planner может быть обычным объектом; важна полнота входной configuration information. НЕ ПЕРЕОБЕЩАТЬ: planner видит только выраженные contracts; он не читает намерения автора и не доказывает semantic equivalence.","m4":"ЗАЧЕМ: привязать narrative к текущему public API и убрать впечатление, что LanguagePlan — выдуманный DTO презентации. СКАЗАТЬ: registry регистрирует packages явно и fail-closed, без assembly scanning. LanguageContributionDescriptor содержит реальные requires/provides capabilities, conflicts, supported backends, transformation, ordering и runtime provider metadata. LanguageDefinitionBuilder задаёт выбранные features/backends и policy overrides. Затем единственный public semantic planner — LanguageCompiler — выполняет feature resolution, contribution resolution и artifact route phase. Результат — LanguageBuildResult, из которого мы получаем immutable LanguagePlan. На плане публичны Features, Contributions, RuntimeProvider, Routes, PlanHash и Summary. Это важная граница ответственности: extension package сообщает локальные факты, planner превращает их в whole-language facts. ПЕРЕХОД: проверим границу на failure case, где «локально всё корректно», но глобально решение неоднозначно. ДЕТАЛЬ: registry identity также сохраняет provenance package registration. Это позже позволяет runtime отвергать implementation, которая выглядит эквивалентной по descriptor, но не является той exact registration, на которой строился plan. НЕ ПЕРЕОБЕЩАТЬ: не утверждать, что каждый возможный composition rule уже выражен; protocol остаётся частью архитектуры.","m5":"ЗАЧЕМ: показать самый конкретный проверяемый failure, а не декоративную диаграмму. СКАЗАТЬ: в current source ResolveCapabilityProvider собирает все eligible providers. Если preferred provider не задан и найдено больше одного кандидата, compiler выдаёт diagnostic UTL2002: capability has multiple providers. Hint прямо предлагает PreferCapabilityProvider. Это важно по двум причинам. Во-первых, порядок регистрации пакетов не становится неявной policy. Во-вторых, исправление находится в LanguageDefinition — то есть host делает глобальный выбор явно, не заставляя один extension знать о другом. В demo я сначала создаю descriptor-only package с consumer и двумя providers; первый Compile завершается UTL2002. Затем добавляю ровно одну policy строку PreferCapabilityProvider и получаю plan. Это настоящая current-source логика, а не презентационная симуляция. ПЕРЕХОД: ambiguity — не единственный глобальный failure. Даже выбранные components могут не образовать маршрут между artifacts. ДЕТАЛЬ: этот failure удобен для тестирования: diagnostic code стабилен и machine-checkable, а correction является частью declarative definition. Поэтому ambiguity можно regression-test без запуска backend или runtime session. НЕ ПЕРЕОБЕЩАТЬ: explicit policy не означает, что planner может сам определить «лучший» provider по смыслу.","m6":"ЗАЧЕМ: показать второй класс глобального решения — artifact routing. СКАЗАТЬ: каждый transformer объявляет source/target LanguageArtifactContract и cost. Backend contribution объявляет свой input contract. Runtime provider объявляет runtime input contracts. Отдельный transformer знает только свою дугу; никто локально не обязан знать полный path. LanguageArtifactRoutePhase строит route на уровне whole-language configuration, а LanguagePlan сохраняет Routes по BackendId, включая Steps и TotalCost. Это и есть место, где «набор независимых возможностей» превращается в конкретный pipeline. Если contract kind или typed identity не состыковываются, route не должен быть додуман runtime-ом. Важно назвать ограничение: ContractsConnect проверяет структурную совместимость kind/value type identity, а не semantic meaning данных. Поэтому route — доказательство выраженного protocol, не доказательство корректности всей программы. ПЕРЕХОД: теперь можно посмотреть на LanguagePlan не как на список, а как на collapsed choice space. ДЕТАЛЬ: cost у transformation влияет на выбор route, но audience не должна путать этот abstract route cost с измеренным execution time. Это planning metric внутри protocol, а не performance benchmark. НЕ ПЕРЕОБЕЩАТЬ: не говорить, что любой graph optimization problem решается автоматически.","m7":"ЗАЧЕМ: дать аудитории ментальную модель plan как продукта разрешения неопределённости. СКАЗАТЬ: before planning у нас есть alternatives и constraints. After planning у нас не «советы runtime-у», а конкретные public fields. Features и Contributions содержат resolved identities пакетов и manifests; RuntimeProvider указывает exact provider/version; Routes закрепляют path для каждого backend; PlanHash вычисляется canonicalizer-ом и verifier проверяет consistency. Именно поэтому plan полезен для reproducibility и lock-file сценария. В regression tests изменение definition-level order меняет PlanHash; перестановка порядка добавления эквивалентных constraints не меняет итоговый order/hash. Это демонстрирует, что planner отвечает не только за «найти что-нибудь», но и за deterministic representation результата. ПЕРЕХОД: если plan уже содержит global decisions, runtime boundary можно сформулировать очень жёстко: runtime materializes and executes this plan, но не делает второй глобальный resolve. ДЕТАЛЬ: reproducibility особенно важна при независимых package updates: если composition inputs изменились, PlanHash/lock representation дают наблюдаемую границу drift вместо молчаливого изменения runtime behavior. НЕ ПЕРЕОБЕЩАТЬ: PlanHash — identity выраженной конфигурации, не security attestation и не semantic hash программы.","m8":"ЗАЧЕМ: это главный architectural slide доклада. СКАЗАТЬ: current source буквально разделяет entrypoints. LanguageCompiler.Compile производит LanguagePlan и в документации говорит, что internal phases never materialize runtimes. LanguageRuntime, наоборот, принимает уже готовый plan. При Create он вызывает LanguagePlanVerifier, проверяет exact runtime provider ID/version, contribution ID, Toolchain API, selected backends/routes и policy. В overload с ILanguageRouteComponentSource assembler materializes exact planned registrations. Затем Run проверяет request против plan и передаёт execution session. То есть runtime validation существует — и должна существовать — но это не second planner. Runtime не выбирает другой provider, если зарегистрирован «почти подходящий». ExactRuntimeBindingTests специально проверяют отказ для equivalent-but-different package registration и materialization only planned components. Это очень сильное deabstraction evidence. ПЕРЕХОД: теперь вернусь к conference title: что именно здесь «исчезает», а что остаётся. ДЕТАЛЬ: это также ответ на testability objection. Planner можно тестировать pure-ish inputs→plan/diagnostics, runtime — plan→materialization/execution; failures разделены по стадии и не смешиваются в одном integration black box. НЕ ПЕРЕОБЕЩАТЬ: validation, plan object, sessions и dispatch остаются реальными runtime costs.","m9":"ЗАЧЕМ: выполнить promise title точно, не превращая его в performance marketing. СКАЗАТЬ: после planning некоторые абстракции перестают быть open questions. До plan capability означает «один из providers», route означает «одна из chains», runtime provider означает «один из candidates». В LanguagePlan это конкретные facts. Поэтому runtime может потреблять selected result, а не носить внутри универсальный composition solver. Я специально формулирую «disappears as a decision». Это сильнее и честнее, чем «объекты исчезают». На machine-code уровне всё ещё могут оставаться interfaces, collections, validation и indirect calls. Отдельный optimization pass потенциально может specialization-ом удалить часть этих механизмов, но текущий доклад не притворяется, что такой benchmark уже проведён. Это различие очень полезно для вопросов про performance: архитектурная staging boundary доказана кодом; speedup требует measurements. ПЕРЕХОД: следующий слайд показывает narrow specialization opportunity и сразу маркирует её как дальнейшую работу. ДЕТАЛЬ: такая формулировка полезна и вне UniversalToolchain: staged configuration может убрать dynamic choice из hot path даже если итоговая implementation всё ещё использует interfaces. Это architectural, а не microbenchmark claim. НЕ ПЕРЕОБЕЩАТЬ: не говорить «zero runtime abstraction overhead».","m10":"ЗАЧЕМ: сохранить связь с compiler engineering и accepted title, но ясно отделить реализованный planner/runtime split от speculative optimization. СКАЗАТЬ: если plan делает slot, type или capability стабильными facts, compiler или IR lowering получает основание специализировать generic representation. Пример LoadEnvironment/Push/LoadExternal иллюстрирует форму возможного rewrite: если slot известен, можно представить более прямую operation. Но этот слайд не является evidence того, что UniversalToolchain master уже выполняет именно такой rewrite или что он даёт конкретный speedup. Для performance claim нужны implementation location, generated code comparison и benchmark methodology. Поэтому на слайде прямо написано future / illustrative. Архитектурная ценность plan не зависит от того, сделаем ли мы эту оптимизацию завтра: staging уже убирает повторное global decision-making. ПЕРЕХОД: теперь demo должен доказать ровно реализованную часть — diagnostic, policy, plan fields и real runtime execution. ДЕТАЛЬ: если specialization станет реальной feature, её evidence contract должен включать before/after IR или generated code, test that semantics preserved и benchmark с workload. Пока этого нет, слайд остаётся hypothesis. НЕ ПЕРЕОБЕЩАТЬ: не использовать этот пример как benchmark или existing optimization evidence."};
